name: lint
on:
  push:
  pull_request:
jobs:
  mathjax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: MathJax delimiter lint
        run: python3 scripts/mathjax_lint.py
