  - id: ANN-20260801-92
    refs: [ANN-20260801-91, ANN-20260801-88, ANN-20260728-83,
      ANN-20260730-84, BET-20260725-12]
    date: 2026-08-02
    note: >-
      FOUR UNGRADED item-0029 SOURCE EXTRACTS LAND, A FIFTH IS WITHHELD
      ON OPERATOR INSTRUCTION; THE RUN HAS STARTED AND BET-20260725-12
      NOW BINDS. Session E of the three-session
      structure ratified at ANN-20260801-91, executed by the executor
      lane (local workstation, Claude Code, model string
      claude-opus-5) on 2026-08-02 against the ephemeral kickoff
      item-0029-kickoff-E-v1.md at pin 1e5a4cc == HEAD of main; web OFF
      and cloud OFF for the whole session. Governing sentence of that
      frame, verbatim from ANN-20260801-91: "Session E (extraction;
      nothing hashed, nothing graded)". FILES OF THIS APPLY, one apply
      per rule 25: four of the five extracts,
      dossier/item-0029-workpapers/extract/maier85-shortintervals.md,
      hildebrandmaier88-gaps.md, freiberg10-strings1.md,
      freiberg11-strings2.md; the run report
      dossier/item-0029-workpapers/item-0029-report-E.md; .gitignore
      (one appended stanza, see the Shiu disclosure below); HANDOVER.md
      (two bounded deltas, enumerated below); this entry. Nothing else.
      THE SHIU EXTRACT IS HELD OUT OF THE TREE ON OPERATOR INSTRUCTION.
      shiu00-strings.md was produced in full to the same specification
      as the other four, is inside its 180-650 envelope at 650 lines,
      and is inventoried in the run report; on operator instruction at
      apply time, taken on publisher-copyright grounds, it is NOT
      committed and is instead named in .gitignore. It exists on the
      executor workstation only. CONSEQUENCE, stated plainly rather
      than absorbed: rule 21 names the anchored extract as "the one
      permitted substitute" for a source that cannot be committed, so
      with the extract withheld the Shiu 2000 source has NO in-tree
      representation at all and remains an object no session reading
      only the tree can read. Session G cannot grade it and Session M
      cannot consume it under rule 26(4); any item-0029 conclusion that
      would rest on Shiu 2000 must therefore either be carried by the
      two Freiberg extracts, which quote and cite Shiu's construction
      at second hand, or be reopened by the operator as a separate
      rule-26(5) event. This entry books the exclusion; it does not
      resolve the standing question it creates.
      THE FOUR COMMITTED EXTRACTS ARE UNGRADED AND CARRY NO CORPUS
      STANDING.
      Rule 26(1), verbatim: "an ungraded or defective extract directs
      attention at most". They are not source surrogates yet; rule 26(4)
      forbids either lane consuming them as such before the Session G
      grade and the hashes that grade licenses. Their rule-27 headers
      carry source identity, sha256, anchor line, bibliographic identity,
      declared extraction method and declared scope, and no grade-state
      and no unresolvable deixis; the stale "re-grade pending" clause of
      the five hashed item-0022 extracts was deliberately NOT imitated.
      Scopes as booked: FULL for Maier 1985, Hildebrand-Maier 1988 and
      1005.4703v2 (and for the withheld Shiu extract); DECLARED PARTIAL
      for 1110.6624v1 (Sections 1-3 in full, Section 4's Selberg-Delange
      proof by named reference only), stated in that extract's header
      and its scope-boundary section.
      NOTHING IS HASHED BY THIS APPLY. payloads/HASHES.txt is
      byte-unchanged; no extract hash, no new source line, no superseding
      line. Standing is conferred by the Session G grade, not by this
      work (rule 26(3)).
      THE RUN-OPENING STATEMENT, per the ANN-88 precedent: the item-0029
      run has STARTED with this apply; BET-20260725-12 NOW BINDS (item
      ratified and run) and stays OPEN for operator judgment against the
      item-0029 finding, resolve_by 2026-09-30; no bet is scored and
      ledger/bets.yaml is untouched.
      V5-V9 FINDINGS, one sentence each, all recorded in the report and
      in the FLAGS of the extract concerned. V5: steering's expectation
      is CONFIRMED against the anchored bytes -- Maier-Primes-in-short-
      intervals.pdf carries no fonts at all and pdftotext returns one
      byte, so there is no text layer and the empty output is the
      expected state rather than a stop (rule 9). V6: steering's
      expectation is CONFIRMED -- S0002-9939-1988-0958032-5.pdf carries a
      text layer from one embedded CID TrueType subset that is
      OCR-corrupt throughout (R^k as "Rfc", N as "TV", p_{n+1}-p_n as
      "Pn+i ~Pn"), so renders are authoritative and the layer was used
      for navigation only. V7: DETERMINED and declared in the header --
      the Shiu publisher PDF has a text layer whose fonts are all Type 1
      CUP subsets with Custom encodings and no ToUnicode map, so
      extracted characters are systematically mis-mapped ("=" as "l", the
      congruence sign dropped), and that source was moved to the
      render-based protocol. V8: page counts in the Section 2.1 line
      order are 9 / 6 / 15 / 24 / 30 against a steering expectation of
      9 / 6 / 15 / ~23-24 / ~29-30, all within expectation, with the one
      detail that Maier 1985's six PDF pages carry printed pages 221-225
      and a blank sixth. V9: CONFIRMED -- the first page of "4. Proof of
      Lemmas 3.2 and 3.3" in 1110.6624v1 is p.13, matching steering's
      read of the non-canonical copy.
      GATES. Every Section 8 gate ran at start and at close and is green
      at both: frozen blocks byte-identical, both relocation checks
      passed, the strict sorry inventory exactly Statement.lean:21, the
      manifest rev present once, the toolchain trailing newline present,
      ledger validate passed at 91 entries, ledger append-only clean over
      the pin..HEAD range, writeup_mapper PASS, mathjax_lint 0 problems
      (143 files at start, 149 at close), roadmap show item-0029 ratified
      both times, and 0 non-ASCII bytes across every new file. No lake
      invocation was made. None of the ten STOP conditions fired; all ten
      are reported by name in the run report.
      ONE PROCESS DEVIATION, disclosed rather than absorbed: the session
      scratchpad holding the page renders was cleared during an operator
      break partway through. The session re-verified the five source
      sha256 a second time, re-rendered all 84 source pages from the
      anchored bytes, re-ran every V5-V8 probe and re-read the already
      drafted pages against the fresh renders before continuing; the two
      affected extracts needed no correction. Every quotation in all five
      extracts rests on a render or text layer produced from the anchored
      bytes and read within this session.
      NEXT STEP: Session G, the fidelity grade over the four committed
      extracts, by a FRESH session over the full surface including the
      prose glosses, hashing on CLEAN only
      (rule 26(1) and 26(3)); on a bounce, one repair session plus one
      terminal re-grade by a further fresh session, at most one cycle.
      Its byte-exact kickoff is authored EPHEMERALLY against the
      then-current pin, per the standing convention; this entry books the
      artifacts, not any kickoff text.
      NOTHING ELSE MOVES. No roadmap file, status, rank, arc or order
      changes; item-0029 remains ratified and the done-move is Session
      M's. No Lean file, frozen block, run, payload text, existing
      dossier workpaper, writeup file, existing extract or extract header
      is touched. No verdict, target, threshold or constant changes, and
      no item-0029 axis, the positive-proportion gate and neither named
      finding is adjudicated anywhere in this apply -- that adjudication
      is Session M's, against graded-clean-and-hashed extracts only.
      HANDOVER DELTAS, two, enumerated. (i) The cold-start read-order
      line moves ANN-01..91 to ANN-01..92. (ii) The item-0029 frontier
      bullet, the one beginning "- FIVE SOURCE ANCHORS FOR item-0029", is
      REWRITTEN to state that four extracts exist UNGRADED under
      dossier/item-0029-workpapers/extract/ with the Shiu extract
      withheld, that nothing is hashed, that
      the run has STARTED and BET-20260725-12 NOW BINDS and stays OPEN,
      that the grade is Session G next, and that a cold start must not
      consume the five as source surrogates before the Session G hashes
      land (rule 26(4)).
      STILL OPEN AFTER THIS ENTRY. Everything ANN-20260801-91 left open,
      carried forward unchanged: BET-20260711-07, BET-20260718-08,
      BET-20260725-12, BET-20260725-13; the item-0010 re-scope (deferred
      until item-0029 and item-0030 return); the two Lean doc surfaces
      routed to item-0027; the item-0006 execution profile, deliberately
      unpriced; source_commit in writeup/sources.yml and the four a292ba4
      heading re-assignments, routed to the next writeup work package;
      ERRATUM-5 booked and deliberately unrepaired; Q1/Q2 of item-0032;
      and the item-0029 kickoff chain, now reduced to G and then M, each
      authored ephemerally on operator request. NEWLY OPEN: the Session G
      grade over the four committed ungraded extracts; the question of
      whether any of the divergences those extracts flag against the
      dispatch attention lists survives a fresh reading of the anchored
      bytes; and the Shiu 2000 standing question created by the operator
      exclusion above -- the source is anchored in payloads/HASHES.txt but
      has no in-tree surrogate, which is exactly the state rules 21 and 26
      exist to prevent, and only the operator can resolve it.
