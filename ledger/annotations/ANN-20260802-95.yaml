  - id: ANN-20260802-95
    refs: [ANN-20260802-94, ANN-20260802-93, ANN-20260801-92,
      ANN-20260801-91, ANN-20260730-84, BET-20260725-12]
    date: 2026-08-02
    note: >-
      THE TERMINAL RE-GRADE LANDS: ALL THREE REPAIRED item-0029 EXTRACTS
      BOUNCE AGAIN AND THE ANN-20260801-91 CYCLE IS SPENT. The second and
      last half of the loop opened at ANN-20260802-93 and half-spent at
      ANN-20260802-94, executed by the executor lane (local workstation,
      Claude Code, model string claude-opus-5) on 2026-08-02 against the
      ephemeral kickoff item-0029-kickoff-G2-v1.md at a Section 0 pin
      equal to HEAD of main; web OFF and cloud OFF for the whole session.
      The session was FRESH in the rule-26(3) sense: it is none of the
      Sessions E, G or R, inherited no context, notes, renders or
      text-layer dumps from any of them, and did not read the withheld
      executor-local shiu00-strings.md. FILES OF THIS APPLY, one apply per
      rule 25: the grade record
      dossier/item-0029-workpapers/extract-grades-G2.md; the run report
      dossier/item-0029-workpapers/item-0029-report-G2.md;
      payloads/HASHES.txt (one appended line, nothing else touched);
      HANDOVER.md (two bounded deltas, enumerated below); this entry.
      Nothing else.
      PER-GRADEE VERDICTS, verbatim from Section 5 of the grade record:
      "maier85-shortintervals.md      VERDICT: DEFECTIVE (1 defect)";
      "freiberg10-strings1.md         VERDICT: DEFECTIVE (2 defects)";
      "freiberg11-strings2.md         VERDICT: DEFECTIVE (1 defect)".
      Zero CLEAN, three DEFECTIVE, four defect rows, by class: (a)
      transcription deviation 2, (b) page-reference error 1, (c) untrue or
      interpretive note claim 1. Classes (d) scope violation, (e) untrue
      header field, (f) forbidden language and (g) untrue FLAGS statement
      are empty across all three. The grade was taken over the FULL
      surface of each extract, not merely the fourteen sites Session R
      repaired; all fourteen were re-checked as ordinary surface and every
      one is correct as applied, so none of the four new rows sits at a
      repaired site. Each row carries file, extract line, source page,
      class, verbatim is-state, verbatim should-state and the finding that
      establishes it.
      THE ONE APPENDED HASHES LINE, the only one Section 4.4 of the
      kickoff licenses at zero CLEAN:
      f4c564b6e4f25cf36e01e9f79b983adeadfa5e314ee1396fdeab6e435063aab1
      dossier/item-0029-workpapers/extract-grades-G2.md. It was
      re-verified against the file on disk after the append, and the
      one-line-per-file invariant was re-checked over the whole file (110
      identifiers, 110 distinct). The three DEFECTIVE extracts got NO
      line; the grade record is hashed in every outcome, including this
      zero-CLEAN one.
      CONSEQUENCE FOR ALL THREE GRADEES. maier85-shortintervals.md,
      freiberg10-strings1.md and freiberg11-strings2.md remain DEFECTIVE
      and therefore "direct attention at most" (rule 26(1), verbatim);
      they carry no hash and neither lane may consume them as source
      surrogates. THE ANN-20260801-91 CYCLE IS SPENT WITH THIS ENTRY and
      no further repair pass may run without a NEW operator-gated
      decision. No extract acquired corpus standing by this apply;
      hildebrandmaier88-gaps.md keeps the standing ANN-20260802-93
      conferred on it and remains the only consumable item-0029 extract.
      THIS SESSION REPAIRED NOTHING AND CONSUMED NOTHING (rule 26(3)). No
      extract was edited, not even for a one-character defect; no extract
      content was used for item-0029 substance -- no axis, no
      positive-proportion gate, no named finding, no comparison to a
      project object; nothing was adjudicated. The grade record is
      terminal: defect rows and nothing beyond them, no repair proposals
      past the should-states the mechanical rule requires, no
      adjudication, no next-step planning.
      OPEN QUESTION FOR THE OPERATOR, created by this outcome and NOT
      resolved here. With the cycle spent and zero CLEAN, the disposition
      of maier85-shortintervals.md, freiberg10-strings1.md and
      freiberg11-strings2.md is the operator's alone. The two alternatives
      are named, not chosen: run Session M on the CLEAN surrogate set the
      verdicts license, which is hildebrandmaier88-gaps.md alone; or open
      a NEW operator-gated repair-and-re-grade cycle over the four rows of
      the G2 record. This entry books the question; it does not answer it.
      THE SHIU EXCLUSION OF ANN-20260801-92 IS CARRIED FORWARD UNCHANGED.
      shiu00-strings.md remains named in .gitignore and exists on the
      executor workstation only; the Shiu 2000 source and that extract
      were out of scope in every direction this session -- not opened, not
      read, not graded, not hashed. The standing question remains the
      operator's: the source is anchored in payloads/HASHES.txt at line
      105 and has no in-tree surrogate, which is exactly the state rules
      21 and 26 exist to prevent.
      BET-20260725-12 STILL BINDS AND STAYS OPEN for operator judgment
      against the item-0029 finding, resolve_by 2026-09-30. No bet is
      scored, re-priced or amended by this apply and ledger/bets.yaml is
      byte-untouched; a fidelity grade over source surrogates is not a
      finding about the item.
      V-G2 OUTCOMES, one line each. V1 freshness: HOLDS, scratchpad empty
      at start. V2: THREE MATCHES, the sha256 of each local source PDF
      equal to its anchor line at payloads/HASHES.txt lines 29, 106 and
      107, verified before any source was opened. V3: P1-P5 all hold; HEAD
      equalled the Section 0 pin with an empty diff, so no rule-18 delta
      arose. V4: 60 pages (6 + 24 + 30) rasterised at 200 dpi in this
      session from the anchored bytes before grading started, plus nine
      400-500 dpi region re-renders, each named in the row it settles.
      V5: reproduced as expected -- Maier 1985 has no fonts and a
      text layer of six form feeds, i.e. none at all (expected state, rule
      9), and both Freiberg layers are clean LaTeX-derived and usable.
      V6: the recount agrees with the report-R Section 4 baseline in every
      column for all three files -- lines 398 / 806 / 499, quoted objects
      11 / 79 / 31, display blocks 30 / 153 / 68, TRANSCRIPTION-UNSURE
      0 / 0 / 0, [extract note] lines 20 / 22 / 20, FLAGS bullets
      6 / 7 / 7 -- so no recount divergence entered the defect tables. One
      PROSE gloss of that baseline is corrected in Section 4 of the record
      without changing any number: the 3 / 1 / 1 surplus-delimiter lines
      are two multi-line closers plus a Section 0 conventions note in
      maier85 and a Section 0 conventions note alone in each Freiberg
      file, so the true multi-line-closer counts are 2 / 0 / 0. No extract
      asserts the gloss, so no defect row follows from it.
      GATES. Every Section 8 gate ran at start and at close and is green
      at both: frozen blocks byte-identical, both relocation checks
      passed, the strict sorry inventory exactly Statement.lean:21, the
      manifest rev present once, the toolchain trailing newline present,
      ledger validate passed, ledger append-only clean over the pin..HEAD
      range, writeup_mapper PASS, mathjax_lint 0 problems over 155 files
      including the two new ones, roadmap show item-0029 ratified both
      times, and 0 non-ASCII bytes across every new or edited file. The
      two hashed session-G artifacts were opened read-only for the P4
      check and still verify against their payloads/HASHES.txt lines at
      close. No lake invocation was made. None of the ten STOP conditions
      fired; all ten are reported by name in the run report.
      THE THREE FIXED D1 READINGS WERE APPLIED, NOT RE-LITIGATED, and no
      finding here rests on rejecting one of them: layout is not graded
      while mathematical grouping is; Section 0 is the declaration place
      for folding conventions and a FLAGS bullet pointing back to it is
      conformant, which position R-A satisfies in all three gradees; and a
      faithful unfolded alphabet rendering needs no declaration. Three
      further judgment calls of this session are disclosed with their
      reasoning in Section 5 of the run report rather than absorbed, so a
      later session can overrule them against the record: the equation
      label spanning a three-line display in Maier 1985 was treated as
      layout; the reflow of a three-row summation subscript into two rows,
      with no content lost, was treated as layout while a dropped
      subscript row was not; and the FLAGS divergence bullets that name
      the extraction brief were not read as project-object language, since
      rule 27 requires the extract to declare its extraction method and
      the bullets carry verifiable source-side claims.
      NOTHING ELSE MOVES. No roadmap file, status, rank, arc or order
      changes; item-0029 remains ratified and the done-move is Session
      M's. No Lean file, frozen block, run, payload text, existing dossier
      workpaper, writeup file, extract, extract header or other extract is
      touched. No verdict, target, threshold or constant changes; no
      item-0029 axis, the positive-proportion gate and neither named
      finding is adjudicated anywhere in this apply.
      HANDOVER DELTAS, two, enumerated. (i) The cold-start read-order line
      moves ANN-01..94 to ANN-01..95. (ii) The item-0029 frontier bullet
      is REWRITTEN to the post-loop state: zero extracts newly CLEAN,
      hildebrandmaier88-gaps.md still the only hashed consumable
      surrogate, the other three DEFECTIVE without standing with the
      ANN-91 cycle SPENT, the operator question open between Session M on
      the CLEAN subset and a new operator-gated cycle, the unchanged Shiu
      exclusion, the unchanged BET-12 binding, and Session M as the next
      step.
      NEXT STEP: SESSION M, its byte-exact kickoff authored EPHEMERALLY on
      operator request, on the surrogate set the verdicts license --
      hildebrandmaier88-gaps.md alone unless the operator opens a new
      cycle first. The ANN-91 loop is closed by this entry in both halves.
      STILL OPEN AFTER THIS ENTRY. Everything ANN-20260802-94 left open,
      carried forward with the loop line closed and one question added:
      BET-20260711-07, BET-20260718-08, BET-20260725-12, BET-20260725-13;
      the item-0010 re-scope (deferred until item-0029 and item-0030
      return); the two Lean doc surfaces routed to item-0027; the
      item-0006 execution profile, deliberately unpriced; source_commit in
      writeup/sources.yml and the four a292ba4 heading re-assignments,
      routed to the next writeup work package; ERRATUM-5 booked and
      deliberately unrepaired; Q1/Q2 of item-0032; the Shiu 2000 standing
      question created by the operator exclusion, unchanged and resolvable
      only by the operator; and Session M, which can consume
      hildebrandmaier88-gaps.md today and none of the other three.
      CLOSED BY THIS ENTRY: the terminal re-grade, and with it the whole
      ANN-20260801-91 bounce loop, whose cycle budget is now SPENT.
      NEWLY OPEN: the operator decision on the three still-DEFECTIVE
      extracts -- Session M on the CLEAN subset versus a new
      operator-gated repair-and-re-grade cycle over the four G2 rows --
      which only the operator can take.
