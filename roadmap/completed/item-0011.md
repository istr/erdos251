---
id: item-0011
status: done
arcs: [research]
rank: "0011"
title: "Hypotheses faithfulness pair: prove singularSeries multipliable and positive"
goal: "singularSeries_multipliable and singularSeries_pos hold for all admissible H, certifying Lean HLQuantA matches paper Hypothesis A"
acceptance_intent: "Both faithfulness lemmata in Hypotheses.lean proved as stated; hypothesis-layer statements unchanged; axiom sets stay within the classical three"
links: [lean/Erdos251/Hypotheses.lean, dossier/chain-v1.md]
execution_profile: { class: middle, reasoning: brief }
---

Contingent owner (M3 discipline: a deferral a gate can see must carry
an item id). Ratify only if item-0003 closes at floor (ANN-14, option
ii-a) with the faithfulness pair still sorried; retire unratified if
the item-0003 stretch discharges both lemmata. Content: multipliable
excludes the tprod junk-value default, so Lean HLQuantA means the paper
hypothesis; pos excludes hidden vanishing clauses that would silently
strengthen it. Route sketch: chain-v1 Lemma 4.1 direction; tail factors
cancel at 1/p order for p beyond the span. See ledger ANN-15.

New scope input from the R3fid arm (ANN-30): the frozen CramerGranville
binds an unconstrained real C (no C >= 1), while prose Hypothesis B
supplies C_g >= 1. The gap is proved INERT at the use site
(delta_le_of_gap_bound: gap n >= 1 always, so hB is unsatisfiable for
C < 0); the faithfulness deliverable should either document this
inertness as the bridge or propose the C >= 1 form -- never touching
the frozen definition unilaterally.

Canary result (item-0015 session 1, ANN-33): the tprod API does not
resist. Both sorries reduce to ONE obligation:
Summable (fun p : Nat.Primes => log (factor p)), with log factor =
O(|H|^2 / p^2), comparable to Nat.Primes.summable_rpow (r := -2);
factor positivity is cheap (nuMod < p, p >= 2). Bridge lemmata:
Real.multipliable_of_summable_log AND Real.rexp_tsum_eq_tprod are BOTH
verified at the mathlib pin, defined side by side in
Mathlib/Analysis/SpecialFunctions/Complex/Log.lean (:265 and :271) with
identical hypotheses (pointwise positivity + Summable of the logs);
positivity of the tprod is then a one-liner via Real.exp_pos. ANN-34
corrects the earlier steering not-located flag (search was not
exhaustive; the lemma sits in the Complex file). The remaining work is analysis,
not API archaeology; schedule as its own small arm or fold into
session 2.

## Final summary

Delivered by co-funding: item-0015 session-2 target T4 proved both faithfulness lemmata as frozen (byte-identity steering-verified, ANN-35), axioms = the classical three, well inside the 2h box. Hypotheses.lean is sorry-free; Lean HLQuantA provably matches paper Hypothesis A (tprod multipliable, hence the mathlib default-1 caveat is void; singularSeries positive). The CramerGranville C>=1 faithfulness question is documented as inert (ANN-30, ANN-34, docstrings); frozen definitions untouched.
