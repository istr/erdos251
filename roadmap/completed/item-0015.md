---
id: item-0015
status: done
arcs: [research]
rank: "0010"
title: "Prove the analytic heart: counting layer, construction, and fork_merge_of_hypotheses"
goal: "fork_merge_of_hypotheses and the 12 Counting.lean statements proved sorry-free, closing the item-0003 floor"
acceptance_intent: "All 13 targeted sorries discharged without reformulating frozen statements; milestone axiom gate stays green; any statement found unprovable as frozen stops work and reports instead of silent repair"
links: [dossier/chain-v1.md, lean/Erdos251/Counting.lean, lean/Erdos251/Conditional.lean, item-0014]
execution_profile: { class: largest, reasoning: high }
---

Drift-invariant planning notes go here.
Round-2 heart funding (ANN-18 successor; round-2 decision 2, ratified).
Consumes the item-0014 statement skeleton: prose sections 4-6 as the 12
Counting.lean sorries plus Conditional.fork_merge_of_hypotheses.
Execution is double-gated: (a) item-0014 closure per its acceptance
letter (blind fidelity verdict), (b) item-0004 verdict (BET-06
investment protection; a literature hit reprioritizes, not devalues,
the formalization). Interface decisions logged at skeleton landing:
transcribe 4.1's x^(1-o(1)) concretely as M_H >= 4 and M_H >> sqrt x
at explicit thresholds feeding 4.3; use the O_kappa(k) collision count
(the 3k / e^2 uniform bounds are retracted). Multi-session,
class=largest; per-session kickoffs are ephemeral against the
then-current pin. Suggested internal order: 4.4 and the section-5 word
lemmata (finite/elementary) before 4.1/4.2/4.3 (analytic), assembly
last. item-0011's faithfulness pair is adjacent and may be co-funded
as a canary in the first session.

Session 1 COMPLETE (ANN-33; executor branch item-0015-counting-proofs,
commit f4cc82b, Counting.lean only, -5/+291): T1-T5 proved (three word
lemmata, cword_admissible, delta_le_of_gap_bound); Counting 12 -> 7
sorries; statements/defs/docstrings/smoke examples steering-verified
byte-identical; helpers in marked proof-layer sections. Session-2
scope proposal: cspan_le via the in-tree Chebyshev.lean route
(centralBinom_le is the hard half; its quadratic nth_prime_lt_sq does
NOT suffice for the C1 * L * log L shape -- extract pi(x) >= c x/log x
and the p_n <= C n log n corollary), then cfm2_tendsto and cbudget;
4.1-4.3 in session 3, assembly last.

Session 2 COMPLETE (ANN-35; branch item-0015-s2, four commits, merge
pending at 807b6b7): all four s2 targets PROVED (cspan_le with C1 = 48,
cfm2_tendsto, cbudget, plus item-0011's pair as T4 -- item-0011 closed
by co-funding). Counting 7 -> 4, project 11 -> 6; the four survivors
are exactly the declared non-goals. Steering verified the report's
diff claim literally (only five deleted lines, all sorry). Findings
booked ANN-35: cfm2_tendsto's hCg is unused (the new hb -- unfreeze
candidate, deferred); T4 landed stronger than routed
(nuMod_eq_card_of_sup_lt needs no primality; the log-series
convergence is unconditional). Method note worth keeping: reduce every
numeric log fact to Real.log_two_gt_d9 by scaling with 2^m so that
log(2^m) = m log 2 is exact; feed nlinarith explicit product hints.
Session-3 scope (kickoff v3): 4.1 (Mertens-free route), the Mertens
pack M1-M3 (mathlib has none at the pin -- steering-verified;
integral-free route in the kickoff; the dyadic M2 route was retired
mid-session for discrete Abel partial summation, executor-caught
coefficient defect, ANN-37, kickoff v3.1), 4.2, 4.3 (five-term Bonferroni
ledger), constr_consCount_pos; fork_merge_of_hypotheses stays s4/s5.

Session 3 COMPLETE (ANN-39; branch item-0015-s3 at 2dc3f65, four
executor commits, merge pending -- fast-forward from b3b445c): T1
singularSeries_lower_bound PROVED (C = 12; k = 0 exact via
singularSeries_singleton_zero); Mertens pack M1-M3 PROVED sorry-free
sharp (M2 coefficient exactly 1, c2 = 1 + b/log 2 - lnln 2; M3
exponent exactly 1; padicValNat_factorial + primorial_le_4_pow route;
M1-lower never needed); T2 step (a) PROVED and the k = 0 edge
discharged in Lean. Counting 4 -> 3, project 6 -> 5; survivors are
exactly 4.2, 4.3, constr_consCount_pos. Session-4 scope: T2 (b)
collision primes < span, (c) split at k+2 (SMALL via M3; LARGE =
sieve-theoretic squarefree-divisor expansion, the substantial piece),
(d) assembly; T3 five-term Bonferroni ledger. Standalone MP refactor
first (operator timing): relocation to dedicated Mertens.lean
(ANN-38) + primesLe/primesUpto unification. Unfreeze candidates (4.1
budget x2, evenness x2) parked in the BET-04 basket.

Session 4 COMPLETE, two commits (executor Claude Opus 4.8). ANN-42
(979df4b) closes T2 whole -- oneExtension_sum_le (Lemma 4.2) PROVED,
route as designed (b/c/d), with two steps found unnecessary (collision
primality; the evens-counting #oneExtensions <= D/2 refinement -- the
crude <= D already closes) and C2(kappa) = 2 kappa mertensC3 exp((2 +
log kappa/log 4)/2) falling out on the first attempt (T1's
constant-tuning precedent did not recur). ANN-43 (bb8a315) lands T3
step 1's structural core as proof-layer helpers
(q_succ_eq_of_no_prime_between, q_add_eq_of_primes,
isConsecRealization_of_primes, the wordPointSet prefix-sum structure,
even_of_admissible, even_of_prime_shift, admissible_of_primes,
mem_oneExtensions_of_prime_shift) -- both of kickoff v4's flagged
step-1 sub-claims are now machine-checked rather than asserted (odd
offsets need no case; the inadmissible-t threshold is cheap, sqrt x >
L+2 suffices). consCount_lower_bound and constr_consCount_pos remain
sorried; project inventory 5 -> 4 = Counting 2 + Conditional 1 +
Statement 1. All three frozen statements (consCount_lower_bound,
constr_consCount_pos, fork_merge_of_hypotheses) steering-verified
byte-identical against the original pin c6c0b98 after both commits;
zero native_decide in either diff.

Scope finding (both steering's kickoff-v4 estimate and the executor's
in-session read agree): the five-term Bonferroni cut is CONFIRMED
SOUND, no route defect -- but T3 is a two-session item, not one
alongside T2. That is a capacity misestimate in kickoff v4, not a
mathematical error; it survived contact. Residual, itemized for
kickoff v5 (T3 alone): (1) step 1's Finset card inequality
(consCount >= tupleCount H x - tupleCount H (sqrt x)
- sum_t tupleCount (insert t H) x), via the anchor map
a |-> Nat.count Nat.Prime a being injective on the good set -- the
ANN-43 structural core supplies the rest; (2) steps 2-3, HLQuantA both
directions + this session's 4.2 + the modelMass (insert t H) x ->
modelMass H x algebra (4.2's span/conclusion shapes line up with 4.3's
hypotheses VERBATIM under kappa = L -- confirmed in-session, no
bridging needed); (3) step 4, tupleCount H (sqrt x) <= sqrt x + 1, no
content; (4) step 5, the x0 assembly -- two growth-rate limits
((lnln x)(lnlnln x)^2/log x -> 0 and x exp(-c(lnln x)^2) >> sqrt x),
easy mathematics but real Lean work, no hidden-coefficient risk since
|H| = O(lnln x) here (not coupled to a summation the way MP-M2's
dyadic blocks were). constr_consCount_pos stays booked "near-free
downstream of T3" as the kickoff's ESTIMATE only -- it sits behind a
sorry and has not been exercised. fork_merge_of_hypotheses remains
entirely untouched, scoped OUT of session 4 by design; item-0003's
floor (ANN-14) stays open on it.

Session 5 COMPLETE (executor Claude Opus 4.8, two commits on main).
COUNTING.LEAN IS SORRY-FREE. ANN-44 (0ed44b4) closes the T3 gate --
consCount_lower_bound (Lemma 4.3) PROVED, kickoff v5's five-term route
built as designed with no deviation; ANN-45 (6055300) closes the
no-gate stretch constr_consCount_pos; ANN-46 records one structural
flag. Project inventory 4 -> 2 = Conditional 1
(fork_merge_of_hypotheses) + Statement 1. All three frozen blocks
byte-identical against the original pin c6c0b98 (the two targets minus
their deleted sorries, which are the deliverable); classical three on
every new declaration and on the milestone axiom gate; no sorryAx in
either target; zero native_decide; lake build green.

Round finding (a simplification, not a repair): bounding ln(L+2) <=
L+1 <= 4t once collapses every ln(4t+2) factor kickoff v5 carried into
a pure power of t = lnln x, so the booked TWO limits plus the
y ln(y+2) monotonicity helper collapse into ONE limit ((ln s)^n =
o(s), from Real.isLittleO_pow_log_id_atTop), used at n = 3 (extension
fraction) and n = 2 (mass margin). L = 0 needed no case split and step
4 needed no lemma, both as booked. ANN-43's open question is RESOLVED:
constr_consCount_pos's "near-free downstream" status was the kickoff's
estimate; it is now a finding, and understated -- 12 lines, no new
helpers, first try, because cspan_le(3,4), cbudget(5) and
cword_admissible plug into 4.3 verbatim. That is a retrospective
vindication of the v1.1/F2 span narrowing.

Two items flagged for steering, not repaired (unfreeze candidates for
the BET-04 basket): (i) oneExtension_sum_le exports C2 with NO SIGN (a
bare exists C2), so the assembly must clamp to max C2 1 before any
monotonicity step -- costless, but 4.2's proof builds C2 > 0 and could
export it; (ii) the glue lemma q_eq_of_count was relocated upward
(statement/docstring/proof byte-unchanged, pointer note left at the
old site per the ANN-38/39/40 convention) because consCount_bonferroni
is its first consumer -- a second inline Nat.nth_count bridge was
rejected since the module docstring asserts q_eq_of_count is the only
glue proof; its docstring's "used only by the smoke tests below" is
now stale in the word "only". Cost note: consCount_lower_bound needs
set_option maxHeartbeats 400000, the tree's first (heartbeats are
cumulative per declaration; the set_option precedes the frozen
docstring per the ANN-26 convention, so the frozen block is untouched).

Item STAYS OPEN on fork_merge_of_hypotheses (s6) alone -- now the only
counting-layer consumer left, with the entire section 4-5 interface
beneath it machine-checked. item-0003's floor (ANN-14, ii-a) is met
except exactly this one theorem.

## Final summary

Closed across s1-s6: all 13 targeted sorries discharged (12 Counting.lean statements plus fork_merge_of_hypotheses); frozen statements byte-identical to the original pin c6c0b98 throughout; every new declaration on the classical three axioms, zero native_decide; heart landed at fc351bf
