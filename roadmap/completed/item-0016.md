---
id: item-0016
status: done
arcs: [infra]
rank: "0020"
title: "Split Counting.lean into themed modules behind an umbrella re-export"
goal: "Counting.lean is a thin umbrella import; its body lives in themed modules with every declaration relocated verbatim"
acceptance_intent: "Pure relocation: concatenation of the new modules reproduces the old body byte-wise modulo module headers and imports; umbrella keeps all downstream imports working unchanged; frozen statements byte-identical in their new homes under a path-independent block check; sorry inventory and axiom gate unchanged; CI green"
links: [lean/Erdos251/Counting.lean, item-0015]
---

Drift-invariant planning notes go here.
Motivation: recurring executor probe-latency signal (scratch file
against the built olean to avoid rebuilding the 2300-line file on every
probe), upstream-port preparation (mathlib-sized themed modules), CI
parallelism, review legibility.

Indicative cut only -- the exact cut belongs to the ephemeral refactor
kickoff, not here: word/point-set defs and basics; singular-series
lower bounds (4.1); one-extension machinery (4.2); consecutive-transfer
core plus the 4.3 statement; tail-bound helpers (4.4); section-5
construction plus the constr statement and smoke tests. Counting.lean
stays as an umbrella that only imports these modules, so
Conditional.lean and all downstream imports see no diff.

Discipline: verbatim relocation per the ANN-40 Mertens precedent;
steering verifies by concatenation diff (new modules concatenated in
declared order reproduce the old body modulo headers and imports); the
frozen-statement byte check becomes path-independent (extract blocks by
anchor, not file path) and HANDOVER plus kickoff templates are updated
to say so. No statement, docstring, proof, or name changes ride along.

Timing (conditional slot, operator decides): the proof-side value
concentrates on Counting-internal iteration, which ends when the two
Counting sorries close. If session 5 leaves Counting work open (T3
spillover or stretch missed), run this split BEFORE that follow-up
session. Otherwise bundle it after heart close together with the
pin-bump and the MP upstream-port preparation. Never mid-session, and
not before session 5 runs (its kickoff is already pinned and
dispatched).

## Final summary

Counting.lean split into themed modules with umbrella re-export completed; relocation verified by concatenation diff and anchor-addressed frozen-block checks (commit ff683a3)
