Operator: Bitte einmal die angehängte Datei lesen. Die sha256-Summe berechnen und ausgeben. Die Anweisungen exakt ausführen. Recherche und Websuche sind ausgeschaltet. Das Ergebnis als Markdown-Datei präsentieren. Das ist eine 0-Temperature, 1-Turn Aufgabe. Keine Nachfragen.

1a_blind.md 81 Zeilen

Numerische Verifikation und Implementierung der Erdős-Problem-Lösung orchestrierend

I'm reading through a file about Erdős Problem #251 concerning the irrationality of a specific infinite series. The user wants me to compute the SHA256 hash of the file and follow the instructions provided, which are in German and specify this is a single-turn task with no web search or follow-up allowed.
I need to deliver rigorous mathematical work on this open problem: verify elementary identities, work through the squeeze mechanism carefully, state a precise conditional theorem, and ideally prove a reduction lemma or identify where the approach gets stuck. The output should be structured with a claims ledger tracking statements and their dependencies, a main writeup, Lean-ready lemma statements where possible, and a section on where progress halts.
Now I'm setting up the core definitions and working through the first identity: expressing the gap series in terms of the original sum, then establishing the tail recursion that will be central to any squeeze argument. Now I'm verifying that the delta term equals the sum of future gaps weighted by powers of two, using a change of variables to reorganize the double sum. This confirms the recursive relationship for the error term. This recursion relation follows directly from the definition: doubling δn\delta_n
δn​ gives gn+1g_{n+1}
gn+1​ plus the remaining weighted sum, which is exactly δn+1\delta_{n+1}
δn+1​. Now I'm moving to the rationality criterion, setting up the condition that SS
S has a specific form with odd denominator, and examining how unu_n
un​ decomposes into a scaled version of SS
S minus an integer combination of the prime gaps. u_n + \text{integer}) \in \mathbb{Q}. So rationality of $S
 is equivalent to integrality of bδnb\delta_n
bδn​ for at least one nn
n.
The integrality condition propagates forward through the recurrence dn+1=2dn−bgn+1d_{n+1} = 2d_n - b g_{n+1}
dn+1​=2dn​−bgn+1​, meaning once dnd_n
dn​ is an integer, all subsequent terms remain integers without imposing any congruence constraints on the gap sequence. This tells me that rationality alone doesn't constrain the dynamics in a meaningful way—I need to appeal to size and Diophantine approximation arguments instead.
Now I'm considering what happens when a run of gaps stays constant: if gn+1=gn+2=⋯=gn+J=cg_{n+1} = g_{n+2} = \cdots = g_{n+J} = c
gn+1​=gn+2​=⋯=gn+J​=c for some length JJ
J, this creates a squeeze mechanism that tightens the constraints. If bδnb\delta_n
bδn​ is an integer, then b∣δn−c∣b|\delta_n - c|
b∣δn​−c∣ must be either zero or at least 1. Using the recurrence relation, this equals 2−Jb∣δn+J−c∣2^{-J}b|\delta_{n+J} - c|
2−Jb∣δn+J​−c∣, so if ∣δn+J−c∣|\delta_{n+J} - c|
∣δn+J​−c∣ is positive but less than 2J/b2^J/b
2J/b, we'd get a contradiction. I also need to consider the case where δn+J\delta_{n+J}
δn+J​ equals cc
c exactly. I need to reconsider the contradiction approach: if δn+J=c\delta_{n+J} = c
δn+J​=c exactly, then by the forward recursion δn+J+1=2c−gn+J+1\delta_{n+J+1} = 2c - g_{n+J+1}
δn+J+1​=2c−gn+J+1​ becomes an integer, which doesn't immediately yield a contradiction. But I should check what bounds apply to the aftermath quantity to see if that forces a contradiction later. So when b≥3b \geq 3
b≥3, the fractional part δm\delta_m
δm​ can't be an integer for m≥sm \geq s
m≥s (since that would force SS
S to be dyadic, contradicting the odd denominator), which means ∣δn+J−c∣≥1/b>0|\delta_{n+J} - c| \geq 1/b > 0
∣δn+J​−c∣≥1/b>0 is guaranteed. This sets up the constraint that a run of length JJ
J at height n≥sn \geq s
n≥s must satisfy ∣δn+J−c∣<2J/b|\delta_{n+J} - c| < 2^J/b
∣δn+J​−c∣<2J/b.
Now for the case b=1b = 1
b=1 where SS
S is dyadic: here δm\delta_m
δm​ is always an integer for m≥sm \geq s
m≥s, so I need 0<∣δn+J−c∣<2J0 < |\delta_{n+J} - c| < 2^J
0<∣δn+J​−c∣<2J. If δn+J≠c\delta_{n+J} \neq c
δn+J​=c, then the distance is at least 1, which gives me a lower bound to work with.
But if δn+J=c\delta_{n+J} = c
δn+J​=c exactly—meaning the weighted average of future gaps equals the run gap—then there's no contradiction from this particular constraint. The question becomes whether δm\delta_m
δm​ can actually equal cc
c for the relevant values. Since δm\delta_m
δm​ is a weighted sum of gaps that grows on average like log⁡m\log m
logm, individual values fluctuate around this trend, so equality is possible but needs careful analysis. If SS
S is dyadic, then for sufficiently large mm
m the δm\delta_m
δm​ values become integers, and the recurrence δm+1=2δm−gm+1\delta_{m+1} = 2\delta_m - g_{m+1}
δm+1​=2δm​−gm+1​ applies. Since δm\delta_m
δm​ is bounded between the minimum and maximum of the future gaps, I need to check whether this constraint forces a contradiction with the requirement that all gaps stay at least 2. ction there. Let me think about the structure differently: if δm\delta_m
δm​ is an integer and equals the sum ∑gm+j2−j\sum g_{m+j}2^{-j}
∑gm+j​2−j, I can relate it to δm+K\delta_{m+K}
δm+K​ by scaling and truncating. The binary expansion of SS
S should eventually stabilize since it's a dyadic rational, but the prime gaps keep growing, which creates tension in how the binary representation propagates carries through the sum.
Actually, I wonder if I can rule out SS
S being dyadic rational entirely. If SS
S were dyadic, then δn\delta_n
δn​ would eventually be an integer for some nn
n. But the recursion δn+1=2δn−gn+1\delta_{n+1} = 2\delta_n - g_{n+1}
δn+1​=2δn​−gn+1​ means gn+1=2δn−δn+1g_{n+1} = 2\delta_n - \delta_{n+1}
gn+1​=2δn​−δn+1​, and since δn\delta_n
δn​ stays at least 2 for all sufficiently large nn
n, this creates constraints on what the gaps can be.
The key insight is that δn\delta_n
δn​ is actually the average of gn+1g_{n+1}
gn+1​ and δn+1\delta_{n+1}
δn+1​—they satisfy δn=(gn+1+δn+1)/2\delta_n = (g_{n+1} + \delta_{n+1})/2
δn​=(gn+1​+δn+1​)/2. So each δn\delta_n
δn​ sits right between the next gap and the next delta value.
Now, gaps are known to grow unboundedly (Westzynthius showed this elementarily via consecutive composites, and Erdős–Rankin gives explicit bounds), but what about how small they can get? Since the work of Zhang and Maynard–Tao, we know unconditionally that lim inf⁡gn≤246\liminf g_n \leq 246
liminfgn​≤246—there are infinitely many prime gaps bounded by 246, which is a deep established result from 2013.
I'm trying to see if I can use this bounded gap property to derive a contradiction in the b=1b=1
b=1 case. If gaps of size at most 246 occur infinitely often, I can set up a recurrence: at those points where gn+1≤246g_{n+1} \leq 246
gn+1​≤246, I get δn≤123+δn+1/2\delta_n \leq 123 + \delta_{n+1}/2
δn​≤123+δn+1​/2, which bounds δn\delta_n
δn​ in terms of the next term but doesn't immediately help. Flipping it around: at small-gap points, δn+1≥2δn−246\delta_{n+1} \geq 2\delta_n - 246
δn+1​≥2δn​−246, which might let me track how the deltas grow.
The doubling map structure is interesting—if δn\delta_n
δn​ gets large relative to the gaps, it roughly doubles. But here's the tension: δn\delta_n
δn​ is a weighted average of future gaps, so it should be bounded by the supremum of those gaps. Yet the gaps are unbounded overall, so there's no pointwise upper bound on δn\delta_n
δn​. Still, δn\delta_n
δn​ itself is always finite and typically scales like log⁡pn\log p_n
logpn​ on average, which creates a constraint I might be able to exploit.
For the b=1b=1
b=1 case with exact integrality, I'm looking at a squeeze argument: if we have JJ
J consecutive equal gaps of size cc
c, then δn+J\delta_{n+J}
δn+J​ must differ from cc
c, and the integrality condition forces ∣δn+J−c∣≥2J|\delta_{n+J} - c| \geq 2^J
∣δn+J​−c∣≥2J. The recurrence δn−c=2−J(δn+J−c)\delta_n - c = 2^{-J}(\delta_{n+J} - c)
δn​−c=2−J(δn+J​−c) means if the δ\delta
δ's are integers, the left side is an integer, which constrains how small the right side can be.
The numerical example at p≈9.8×106p \approx 9.8 \times 10^6
p≈9.8×106 with J=4J=4
J=4 and c=30c=30
c=30 is striking—the aftermath difference sits right at the threshold with ∣δn+4−30∣≈16≈24|\delta_{n+4} - 30| \approx 16 \approx 2^4
∣δn+4​−30∣≈16≈24, which is marginal but consistent with the theory.
Now I need to formulate the precise conditional theorem rigorously. The key hypothesis is that for every JJ
J, there exist infinitely many runs of equal gaps of length at least JJ
J where the aftermath deviation ∣δn+J−c∣|\delta_{n+J} - c|
∣δn+J​−c∣ is bounded by Alog⁡pnA \log p_n
Alogpn​ for some absolute constant AA
A—this avoids the unknown constant bb
b and gives a cleaner statement to work with. n+J} - c| < 2^J/b
 for some unknown bb
b. The key insight is that we need runs at arbitrarily large positions to overcome the exponential growth, so the conditional theorem requires: for every threshold TT
T, there exist a run of length JJ
J starting at position n≥Tn \geq T
n≥T with controlled deviation from the run value, where the deviation bound depends on JJ
J and an unknown parameter bb
b.
Now I'm completing the proof by contradiction—if SS
S were rational with denominator 2sb2^s b
2sb where bb
b is odd, I can choose TT
T large enough to capture both ss
s and bb
b, then use the run condition to derive a contradiction about the divisibility of bδnb\delta_n
bδn​.
The strict inequality in condition (ii) is crucial for handling dyadic rationals uniformly. I'm noting that while the hypothesis is self-referential in terms of the δ\delta
δ sequence, it still functions as a valid reduction, though it would be cleaner to connect this to established conjectures about prime gaps.
Now I'm working through bounds on the aftermath term δn+J−c\delta_{n+J} - c
δn+J​−c. Using the binary expansion, I can express δn+J\delta_{n+J}
δn+J​ as a sum and bound it in terms of the maximum digit and a tail term. The key insight is that averaging δm\delta_m
δm​ over all m≤Nm \le N
m≤N gives roughly log⁡N\log N
logN, and by Chebyshev's inequality, at least half the values satisfy δm≤2δˉ\delta_m \le 2\bar{\delta}
δm​≤2δˉ.
The challenge is that I need this bound not just on average but specifically at the sparse locations where runs of length JJ
J occur. The Hardy–Littlewood estimates give the count of such runs as roughly N/(log⁡N)J+1N/(\log N)^{J+1}
N/(logN)J+1 times a singular series factor, and conditioning on the aftermath being bounded removes at most half of these. This requires a more refined statement about the gap structure after each run — essentially an "almost all" version that controls the tuples simultaneously.
So I'm defining a concrete hypothesis: for any run of JJ
J consecutive primes with equal gaps cc
c, the aftermath δ\delta
δ is the weighted sum of subsequent gaps, and I need to formalize that every JJ
J admits at least one such run with controlled aftermath. So if I extract a sub-run of length JJ
J from a longer run, the aftermath stays the same and the threshold only gets better. Now I'm considering whether there's an unconditional result I can establish—something that doesn't depend on the Collatz conjecture. The main candidate is ruling out b=1b = 1
b=1, which would mean S∉Z[1/2]S \notin \mathbb{Z}[1/2]
S∈/Z[1/2]. This requires finding some δn\delta_n
δn​ that's not an integer, which is equivalent to showing the binary expansion of SS
S doesn't eventually stabilize.
That's a hard problem because it amounts to proving SS
S has infinitely many 0s and 1s in its binary representation, and since those digits come from the intricate carry structure in the Collatz dynamics, I don't see a known technique for it unconditionally. But maybe I can leverage the numerical data from the J=4J=4
J=4 run—a single run gives me that if δ\delta
δ is integral (meaning b=1b=1
b=1 and s≤ns \le n
s≤n), then either ∣δn+4−30∣≥16|\delta_{n+4} - 30| \geq 16
∣δn+4​−30∣≥16 or δn+4=0\delta_{n+4} = 0
δn+4​=0, and I can check this numerically.
The key insight is that δn+4\delta_{n+4}
δn+4​ is a computable real number determined entirely by the actual primes, so if I calculate it to high precision and find that δn+4−30\delta_{n+4} - 30
δn+4​−30 falls strictly in the interval (0,16)(0, 16)
(0,16), that would prove the negation—that SS
S is not a dyadic rational with denominator 2s2^s
2s for any s≤ns \le n
s≤n, which is a finite computational result showing SS
S cannot be rational with denominator bounded by roughly 2650,0002^{650,000}
2650,000. So by computing δn\delta_n
δn​ for a sufficiently large index nn
n (using primes up to 3×1073 \times 10^7
3×107 with rigorous tail bounds), I can check whether bδnb\delta_n
bδn​ is close to an integer for all odd b≤Bb \le B
b≤B. If the distance exceeds the tail error for any such bb
b, that rules out S=a/(2sb)S = a/(2^s b)
S=a/(2sb) with s≤ns \le n
s≤n, giving an explicit lower bound on the denominator height of any rational representation. The continued fraction expansion confirms this rigorously—if SS
S were rational, its CF would terminate, but computing it to sufficient depth with verified partial quotients shows the denominator must exceed any bound we can compute, which is a standard consequence already implicit in the CF numerics. So this doesn't really constitute new progress toward the actual goal.
For the unconditional results on equal consecutive gaps, I need to be careful here. The computational evidence is only calibration-grade, not a proof. What's actually known unconditionally is that sign changes in gn+1−gng_{n+1} - g_n
gn+1​−gn​ occur infinitely often (Erdős–Turán 1948), but whether equal consecutive gaps appear infinitely often remains open—I'm fairly confident this is still an unsolved problem, despite Erdős's conjectures about gap patterns.
On the consecutive primes in arithmetic progression front, there's a distinction between general k-term APs of primes (solved by Green–Tao) and the much harder problem of consecutive primes forming APs. Shiu proved results about strings of consecutive primes satisfying congruence conditions, and more recent work by Banks and collaborators using Maynard–Tao methods has pushed further on this, though the full picture of what's proven versus open isn't entirely clear to me from memory. For any admissible tuple, the Banks–Freiberg–Turnage-Butterbaugh result (using Maynard–Tao machinery) establishes that such patterns appear infinitely often among consecutive primes, though forcing all elements of a tuple to be prime simultaneously while excluding primes between them is the harder constraint—but there's a known result about infinitely many triples satisfying this. I need to be careful here—I'm relying on memory for these claims about consecutive primes and related results, so I should mark them as unverified rather than presenting them as established fact. Now I'm considering alternative approaches to prove the irrationality of SS
S: the Furstenberg normality angle seems tricky since showing the binary digits aren't eventually periodic requires understanding how carries work, and the natural digit-blocking argument probably fails anyway. The Fourier-Weyl route would require proving equidistribution of {2nS}\{2^n S\}
{2nS}, but that's a much stronger condition and doesn't work in general since rationals are counterexamples.
Actually, I've found a cleaner reformulation: the fractional parts of 2nS2^n S
2nS equal the fractional parts of δn\delta_n
δn​ since the integer parts cancel out. This gives me a precise criterion—SS
S is rational if and only if there exists some positive odd integer bb
b such that bδnb\delta_n
bδn​ becomes an integer for all sufficiently large nn
n, which is equivalent to {δn}\{\delta_n\}
{δn​} being eventually periodic in a specific sense. SS
S is rational exactly when some δn\delta_n
δn​ is rational, which means all of them are since they differ only by rationals. So SS
S is irrational if and only if δ1\delta_1
δ1​ is irrational.
Now I'm exploring a different angle: using a Liouville-type inequality that exploits how runs create local structure in the binary expansion, or alternatively viewing 2S2S
2S's digits through a lim sup⁡\limsup
limsup lens. There's also a base-2 approach mirroring Erdős's 1958 factorial proof, where he showed ∑pn/n!\sum p_n/n!
∑pn​/n! is irrational by leveraging superexponential growth of factorial denominators relative to numerators—in factorial base, the digit of SS
S becomes essentially pnp_n
pn​ modulo small values, and Bertrand's postulate gives digit control.
For base 2, the core obstacle is that pn≈nlog⁡np_n \approx n \log n
pn​≈nlogn vastly exceeds the base spacing of 2 per step, causing massive carries. Even if I can't prove full irrationality, maybe I can at least show infinitely many binary digits of SS
S are 1—that is, SS
S is not dyadic. SS
S is dyadic only if eventually all digits become 0 or all become 1. Looking at the fractional parts {δn}\{\delta_n\}
{δn​} where δn={2nS}\delta_n = \{2^n S\}
δn​={2nS}, SS
S is dyadic precisely when {δn}=0\{\delta_n\} = 0
{δn​}=0 eventually. If δn\delta_n
δn​ is an integer for all n≥sn \geq s
n≥s, then the recurrence gn+1=2δn−δn+1g_{n+1} = 2\delta_n - \delta_{n+1}
gn+1​=2δn​−δn+1​ imposes gap identities that might lead to a contradiction.
Now I'm checking the parity constraints: since δn+1=2δn−gn+1\delta_{n+1} = 2\delta_n - g_{n+1}
δn+1​=2δn​−gn+1​, we have δn+1≡gn+1(mod2)\delta_{n+1} \equiv g_{n+1} \pmod{2}
δn+1​≡gn+1​(mod2), and for n+1≥2n+1 \geq 2
n+1≥2, the gap gn+1=pn+2−pn+1g_{n+1} = p_{n+2} - p_{n+1}
gn+1​=pn+2​−pn+1​ is even (difference of odd primes), which forces δn\delta_n
δn​ to be even for n≥2n \geq 2
n≥2.
This means I can define en=δn/2e_n = \delta_n/2
en​=δn​/2 with the recurrence en+1=2en−gn+1/2e_{n+1} = 2e_n - g_{n+1}/2
en+1​=2en​−gn+1​/2, though the gaps modulo 4 don't give further parity constraints. The key question is what δn\delta_n
δn​ converges to—we expect it to grow like log⁡n\log n
logn on average, which is consistent with integrality, but I need to check what unconditional results on prime gaps tell us. At huge-gap points, the recurrence gives δn≥gn+1/2+1\delta_n \ge g_{n+1}/2 + 1
δn​≥gn+1​/2+1, which means the offset is always at least half the next gap plus one. The real constraint is that ruling out b=1b=1
b=1 unconditionally seems to require either finding a specific run or discovering a fundamentally new approach, since combinatorially there are plenty of integer sequences with δn≥2\delta_n \ge 2
δn​≥2 that could generate plausible gap patterns—the hard part is that only primality of the partial sums actually restricts which ones work.
Now I want to formalize the vacuity argument rigorously: for any b≥1b \ge 1
b≥1 and starting index n0n_0
n0​, if bx∈Zbx \in \mathbb{Z}
bx∈Z for some real xx
x, then the forward orbit under the recurrence preserves bδn∈Zb\delta_n \in \mathbb{Z}
bδn​∈Z, which means the set of all gap sequences compatible with this divisibility condition actually imposes no real constraint on the gaps themselves.
The key insight is that rationality is fundamentally a tail property—it can't be detected or refuted by looking at finitely many gaps. Given any gap sequence and any bb
b, I can always construct a starting value δn0\delta_{n_0}
δn0​​ by summing the gaps with binary weights, and this single real-number choice makes all the divisibility constraints consistent. The forward recursion then shows that satisfying the constraint at one step is equivalent to satisfying it at all subsequent steps, so no finite pattern of gaps can ever force irrationality. the tail sum can achieve any value in a certain interval by choosing the gap values from {2, 4}, which gives us enough flexibility to hit the required congruence condition modulo 1/b. The tail sum can range over [2⋅2−N,∞)[2\cdot2^{-N}, \infty)
[2⋅2−N,∞) by choosing from even numbers unboundedly, which guarantees hitting any required residue class. This rigorously proves that no finite gap pattern contradicts rationality even when bb
b is known, eliminating all finite-certificate strategies. For the next reduction, I'm exploring whether the squeeze argument can work with something weaker than exact equality of consecutive gaps, moving closer to established techniques.
Now I'm setting up a generalized framework using deviations from a center value cc
c, which telescopes nicely through the recurrence relation. The key insight is that we don't actually need the gaps to be exactly equal—instead, the weighted sum of deviations across the window, combined with the tail term, just needs to be small but nonzero modulo 1/b1/b
1/b.
This reduces the irrationality problem to a single criterion: for every denominator bb
b, there exists some position nn
n where the fractional part of bδnb\delta_n
bδn​ is strictly between 0 and 1. Since δn\delta_n
δn​ itself is the binary expansion ∑jgn+j2−j\sum_j g_{n+j}2^{-j}
∑j​gn+j​2−j, the irrationality of SS
S is equivalent to the irrationality of this single real number formed by the gap sequence.
The run-squeeze construction is just one concrete way to force the first JJ
J binary digits of δn−c\delta_n - c
δn​−c to vanish, certifying the nonzero fractional part condition. I'm now exploring how near-geometric gap patterns might generalize this approach. I'm exploring a cleaner pattern where most terms equal cc
c and only the final term deviates, which simplifies the weighted sum into a dyadically small correction that merges into the aftermath. This suggests the core hypothesis: for any bound BB
B, I can find nn
n and integer cc
c where δn\delta_n
δn​ sits strictly between cc
c and c+1/Bc + 1/B
c+1/B, capturing the essential nonvanishing and smallness requirement.
Now I'm recognizing that small distance to integers means δn\delta_n
δn​ approaches integer values, so the real criterion for irrationality is when this distance shrinks infinitely often. I'm formulating a master lemma: if infinitely many nn
n satisfy 0<dist(δn,Z)<ϵn0 < \mathrm{dist}(\delta_n, \mathbb{Z}) < \epsilon_n
0<dist(δn​,Z)<ϵn​ with ϵn→0\epsilon_n \to 0
ϵn​→0, then SS
S must be irrational, and I'm checking this against the rational case where S=a/(2sb)S = a/(2^sb)
S=a/(2sb) to verify the logic holds. δn,Z)<1/b\delta_n, \mathbb{Z}) < 1/b
δn​,Z)<1/b, then SS
S is irrational. This is equivalent to saying the liminf of the distance to integers approaches zero. For a general odd bb
b, I need to formalize this condition more carefully—specifically, I want to contradict the assumption that δn\delta_n
δn​ lies in 1bZ\frac{1}{b}\mathbb{Z}
b1​Z by showing the distance to Z\mathbb{Z}
Z can be arbitrarily small.
If δn=k/b\delta_n = k/b
δn​=k/b for some integer kk
k, then its distance to Z\mathbb{Z}
Z is j/bj/b
j/b where 0≤j≤b/20 \le j \le b/2
0≤j≤b/2. Requiring this distance to be less than 1/b1/b
1/b forces j=0j = 0
j=0, meaning δn∈Z\delta_n \in \mathbb{Z}
δn​∈Z, which contradicts the strict positivity of the distance. So the characterization works: SS
S is irrational if and only if for every ϵ>0\epsilon > 0
ϵ>0 and every NN
N, there exists n≥Nn \ge N
n≥N with 0<dist⁡(δn,Z)<ϵ0 < \operatorname{dist}(\delta_n, \mathbb{Z}) < \epsilon
0<dist(δn​,Z)<ϵ.
For the forward direction, if SS
S is irrational, I need to show such nn
n exist. The fractional part {2nS}\{2^nS\}
{2nS} determines the distance to Z\mathbb{Z}
Z as the minimum of {2nS}\{2^nS\}
{2nS} and 1−{2nS}1 - \{2^nS\}
1−{2nS}. However, for irrational SS
S, the liminf of these distances isn't necessarily zero—there exist badly-approximable numbers under doubling whose orbits avoid zero entirely, such as certain numbers with specific binary expansions.
So the equivalence breaks down; the condition is only sufficient. Theorem R is one-directional: (H*) implies irrationality, but irrationality doesn't guarantee (H*) for arbitrary reals. For our specific SS
S, though, it might hold. The key reduction is that rationality forces the distances to stay bounded away from zero in a specific way. Let me formulate this precisely: if SS
S is rational with odd denominator bb
b, then from some point onward all δn\delta_n
δn​ lie in 1bZ\frac{1}{b}\mathbb{Z}
b1​Z.
The contrapositive gives us a certificate for irrationality: if we can show that 0<dist⁡(δn,Z)<1/b0 < \operatorname{dist}(\delta_n, \mathbb{Z}) < 1/b
0<dist(δn​,Z)<1/b holds infinitely often for every bb
b, then SS
S must be irrational. The run-squeeze has been our only known way to generate such certificates, but I'm realizing the certificate doesn't have to come from runs at all—it's really about how the fractional parts of δn\delta_n
δn​ relate to the binary structure and the gaps appearing in the window.
I can reformulate this combinatorially: define a finite window sum Wn(K)W_n(K)
Wn​(K) that captures the weighted binary digits up to position KK
K, then decompose δn\delta_n
δn​ as this window plus a remainder term scaled by 2−K2^{-K}
2−K. The key insight is finding windows where Wn(K)W_n(K)
Wn​(K) lands close enough to an integer (within 2−K2^{-K}
2−K scale) after accounting for what comes after. The run-squeeze is just the special case where the gap sequence is constant, but there might be other window configurations that work.
Now I'm looking at a different certificate approach using differences: if I compare δn−δm\delta_n - \delta_m
δn​−δm​ for two different positions, the rationality condition forces b(δn−δm)b(\delta_n - \delta_m)
b(δn​−δm​) to be an integer. When the gap sequences agree for JJ
J consecutive steps, the difference factors as 2−J2^{-J}
2−J times another difference, which constrains how large the remaining gap can be relative to bb
b.
The key insight is that this actually requires something weaker than constant gaps — just repeated gap patterns appearing at different positions. By the pigeonhole principle, if I look at windows of JJ
J consecutive gaps bounded by some value MM
M, there are only finitely many possible patterns (specifically (M/2)J(M/2)^J
(M/2)J patterns of even gaps), so with enough windows, two must match. This means repeated patterns are essentially guaranteed to exist without any special assumptions.
Now, Maynard's theorem gives me exactly what I need: for any mm
m, there are infinitely many positions where mm
m consecutive gaps each stay bounded by some constant C(m)C(m)
C(m). Since there are only finitely many possible patterns of bounded gaps, the pigeonhole principle guarantees that some pattern must repeat infinitely often — and this follows unconditionally from Maynard's result.
So I can fix a pattern and find infinitely many positions n1<n2<…n_1 < n_2 < \dots
n1​<n2​<… where the gaps match this pattern exactly. For any two such positions nin_i
ni​ and nkn_k
nk​, the quantity b(δni+J−δnk+J)b(\delta_{n_i+J} - \delta_{n_k+J})
b(δni​+J​−δnk​+J​) must be an integer, which creates a contradiction if the difference between these shifted δ\delta
δ values falls in a certain small range. The key is understanding what constraints apply to the sequence of shifted δ\delta
δ values at these infinitely many positions. The weighted gap sums could coincide even if the tail sequences differ—this is just a codimension-1 constraint, totally possible under rationality. So I can't rule out exact coincidences of δn+J=δm+J\delta_{n+J} = \delta_{m+J}
δn+J​=δm+J​ just from the structure. This means the unconditional pigeonhole argument gives me a new theorem: if SS
S is rational with denominator 2sb2^s b
2sb (where bb
b is odd), then for every JJ
J, something must happen along the infinitely many pattern-repeat locations. The key insight is that this reduction shifts the hard part from finding equal gap runs to understanding when aftermath values coincide or separate. I'm checking that the pigeonhole argument holds: Maynard-Tao guarantees infinitely many nn
n where pn+m−pnp_{n+m} - p_n
pn+m​−pn​ stays bounded by some function C(m)C(m)
C(m), which means repeated patterns exist unconditionally for any pattern length JJ
J—this is the crucial unconditional existence that makes the reduction work. ollary follows: for any choice of JJ
J, there's a JJ
J-tuple of even gaps that appears infinitely often in the sequence. Now I'm setting up the two-window identity, which relates the differences between δ\delta
δ values at two pattern locations using a telescoping sum involving the gaps, ultimately reducing to a comparison of shifted δ\delta
δ values under the rationality assumption.
From this, I can establish that the difference between any two δ\delta
δ values (for indices beyond some threshold ss
s) must be either zero or bounded below by 2J/b2^J/b
2J/b, which severely constrains the possible values. I'm now formulating a weaker conditional theorem that avoids the full strength of the CPAP hypothesis, though I need to be careful about how to quantify the existence of JJ
J relative to the parameter TT
T.
The key insight is that for any pattern of length JJ
J appearing infinitely often, pigeonhole principle guarantees we can find two distinct positions nn
n and mm
m (both sufficiently large) sharing that pattern but with their δ\delta
δ values separated by a gap strictly between zero and 2J/T2^J/T
2J/T—and this should hold for every choice of TT
T.
This hypothesis directly implies SS
S is irrational. Heuristically, the aftermath values δn+J\delta_{n+J}
δn+J​ at repeat locations behave like independent Gumbel-type random variables of size roughly log⁡pn\log p_n
logpn​; Maynard's result guarantees infinitely many repeats at unknown heights, but eventually the typical aftermath size log⁡pn\log p_n
logpn​ grows much larger than 2J2^J
2J for any fixed JJ
J, creating the same quantitative tension we saw before—except now we sidestep the need for CPAPs to exist.
The key insight is that we need aftermath values to come close without coinciding. Since the aftermath distribution spreads over [2,O(log⁡)][2, O(\log)]
[2,O(log)], we face a pigeonhole constraint: infinitely many values that are either pairwise dd
d-separated or equal, all at least 2, means at most 1+(X−2)b/2J1 + (X-2)b/2^J
1+(X−2)b/2J distinct values below any threshold XX
X—but the same value could repeat infinitely often, creating a coincidence problem.
So rationality forces a dichotomy: for every JJ
J, either the aftermath multiset at JJ
J-repeats has aftermaths tending to infinity along repeats, or we get an exact real coincidence where δn+J=δm+J\delta_{n+J} = \delta_{m+J}
δn+J​=δm+J​ for n≠mn \ne m
n=m. When this exact coincidence occurs, subtracting gives a dyadic relation between two different tails of the gap sequence, which constrains what's possible.
Now I'm checking whether such exact coincidences can actually happen. If δN=δM\delta_N = \delta_M
δN​=δM​ with N<MN < M
N<M, I can define the difference Dk=δN+k−δM+kD_k = \delta_{N+k} - \delta_{M+k}
Dk​=δN+k​−δM+k​ starting from D0=0D_0 = 0
D0​=0, and this satisfies a recurrence relation involving the gap differences. Expanding this recurrence shows that DkD_k
Dk​ is a weighted sum of the gap differences between positions NN
N and MM
M.
The key constraint is that DkD_k
Dk​ must stay bounded—it can't exceed the maximum possible delta values. But the recurrence relation amplifies the gap differences exponentially unless they decay very precisely. This means for two sequences to have exactly equal deltas at different starting positions, their gap sequences must satisfy an extremely tight quantitative condition: the weighted partial sums of gap differences must decay like 2−k2^{-k}
2−k, which severely restricts what's possible.
Now I'm tracking what happens when the gap sequences first disagree. If the first difference appears at position i0i_0
i0​ with magnitude at least 2, then Di0D_{i_0}
Di0​​ jumps to at least 2 in absolute value, and this gets amplified exponentially in subsequent steps unless something cancels it out.
The key insight is that coincidences occur when the weighted gap sums align perfectly — meaning the fractional parts of 2NS2^N S
2NS and 2MS2^M S
2MS must be equal, which happens exactly when the delta sequences match. This confirms that SS
S must be rational with a very specific form: if S=a/(2sb)S = a/(2^s b)
S=a/(2sb) in lowest terms, then a coincidence where δN=δM\delta_N = \delta_M
δN​=δM​ for M<NM < N
M<N occurs precisely when bb
b divides 2N−M−12^{N-M} - 1
2N−M−1, which ties back to the divisibility condition 2M(2N−M−1)S∈Z2^M(2^{N-M}-1)S \in \mathbb{Z}
2M(2N−M−1)S∈Z. ≥s\ge s
≥s and the multiplicative order of 22
2 modulo bb
b divides N−MN - M
N−M. This is striking: the sequence becomes periodic with period equal to that order once we reach index ss
s. So if SS
S is rational, the tail of the sequence (δn)(\delta_n)
(δn​) stabilizes into a repeating pattern determined by the denominator's odd part.
Let me verify this more carefully by working through the algebra: expressing SS
S in lowest terms as a/(2sb)a/(2^s b)
a/(2sb) with bb
b odd, then computing (2n+d−2n)S(2^{n+d} - 2^n)S
(2n+d−2n)S where dd
d is the multiplicative order of 22
2 modulo bb
b. Since bb
b divides 2d−12^d - 1
2d−1, this difference becomes an integer for all n≥sn \ge s
n≥s, which forces the periodic behavior in δn\delta_n
δn​.
The key insight is that δn+d−δn\delta_{n+d} - \delta_n
δn+d​−δn​ is actually an integer, not zero—I was conflating periodicity with the difference being zero. What's actually periodic is the fractional part {δn}\{\delta_n\}
{δn​}, which equals {2nS}\{2^n S\}
{2nS} for rational SS
S.
For exact equality δN=δM\delta_N = \delta_M
δN​=δM​, I need (2N−2M)S(2^N - 2^M)S
(2N−2M)S to be an integer, plus the gap and interval differences must also align. The fractional parts cycle with period d=ordb(2)d = \mathrm{ord}_b(2)
d=ordb​(2), but the full values differ by integer amounts tracking the cumulative gaps.
Now applying the two-window pigeonhole argument: when JJ
J-patterns repeat at positions nn
n and mm
m (both ≥s\geq s
≥s), the scaled difference b(δn+J−δm+J)b(\delta_{n+J} - \delta_{m+J})
b(δn+J​−δm+J​) lands in 2JZ2^J\mathbb{Z}
2JZ, and the resulting aftermath values are confined to 1bZ∩[2,∞)\frac{1}{b}\mathbb{Z} \cap [2,\infty)
b1​Z∩[2,∞) with pairwise congruences modulo 2J2^J
2J. So the key insight is that if we can find two occurrences of the same J-pattern at positions n and m (both beyond some threshold T) where the aftermath values differ but stay within a window of size 2^J/T, we'd get a contradiction. This suggests a separation hypothesis: for any J and T, there must exist some length-J pattern occurring infinitely often with at least two occurrences having distinct aftermath values that are close together—specifically, their difference is less than 2^J/T. This contrasts with the earlier CPAP hypothesis.
Now I'm thinking about whether this separation condition is actually weaker structurally. The pattern existence part becomes almost free thanks to Maynard-Tao and pigeonhole arguments, shifting all the real difficulty to the analytic side: proving aftermath values stay close while remaining distinct. Under rationality assumptions, two aftermath values coincide exactly when a certain integer condition holds—when the difference (2^{n+J} - 2^{m+J})S equals some specific value. This is possible under rationality, but there might be a clever strengthening here.
If all the aftermath values at repeat positions equal the same real number, then infinitely many indices would share that value, and the recursion would be determined forward by the gaps between them. Even with two indices N and M having equal aftermath values, their forward orbits would be determined by those gaps, and there's no immediate contradiction. But under rationality, equal aftermath values only occur when b divides 2^{M-N} - 1, which restricts which indices can actually coincide.
The CPAP-run case is really just the two-window version where the second window is the first window shifted by one position—overlapping windows where the pattern repeats over a length-J-1 interval.
This gives a clean structural picture: overlapping windows lead to the CPAP squeeze, while disjoint windows give the pigeonhole version. The disjoint case works unconditionally (pattern existence is guaranteed), but the overlapping case depends on CPAP (still open). Now I'm wondering if we can push the unconditional part further—we know unconditionally that for any J there exists a pattern with infinitely many occurrences, and under rationality the aftermaths at those occurrences cluster in a coset of (2^J/b)ℤ, all at least 2.
For each J, I can choose it large enough so that 2^J/b exceeds some threshold, which means infinitely many aftermath values land in a (2^J/b)-spaced grid. The aftermath values constrain later gaps through bounds like δ_{n+J} ≥ g_{n+J+1}/2 + 1, so if I can show the next gap g_{n+J+1} is bounded infinitely often along the occurrence sequence, maybe I can apply Maynard again with pattern length J+1 to get both the repeating pattern and its length-J prefix repeating.
When the (J+1)th gap is fixed at h_{J+1}, the aftermath relation becomes δ_{n+J} = h_{J+1}/2 + δ_{n+J+1}/2, which means differences between aftermaths at level J are just half the differences at level J+1 — this is consistent with the spacing constraint but doesn't give me new information.
Now I'm considering values in the set θ + (2^J/b)ℤ that are at least 2, and asking whether infinitely many of them repeat or whether they grow unboundedly. I'm setting up a recurrence relation for the differences between two gap sequences at positions offset by k, tracking how these differences evolve and whether they remain bounded or grow unbounded through the doubling mechanism, constrained by the gap differences themselves.
The key insight is that the two-window pigeonhole reduction forms the core contribution here. Now I need to verify everything computationally — checking the identities, the recursion behavior, the squeeze bounds, the two-window identity itself, and computing S to high precision along with the continued fraction expansion. I should also validate the specific run at p=9,843,019 with J=4 to confirm those five consecutive primes in arithmetic progression with difference 30, plus verify the SHA256 hash and all the numeric claims in the dossier.
I'm verifying that 9843019, 9843049, 9843079, 9843109, and 9843139 are all prime and consecutive, and checking that log₂(log p) ≈ 4.01 holds for this value. Now I'm wondering whether the two-window pigeonhole reduction is genuinely novel or just a natural approach — I have a vague recollection that Erdős problem 251 might be exactly this, with some recent activity around 2023-2025 on the Erdős problems website and Tao's blog discussing finite intersectivity.
I'm trying to pin down what's actually known here. There's definitely a classical result about the irrationality of ∑ pₙ/2ⁿ, but I'm not certain if it's proven or still open. I'm also conflating several related results in my head — something about Erdős or others proving irrationality of divisor function series like ∑ σ(n)/2ⁿ or ∑ d(n)/2ⁿ, possibly connected to ∑ 1/(2ⁿ − 1), and I recall Kevin Pratt having recent work in this area, though I'm fuzzy on the exact statement. ember Tao proved something around 2024/2025 related to an Erdős problem, possibly about sums like ∑ 1/(2^n n), though I can't verify the exact details given my knowledge cutoff. The dossier treats it as open, so I'll note the possibility of recent work I can't confirm rather than inventing citations.
As for ∑ p_n 2^{-n} irrationality, that remains open as far as I know. There are classical results on related series like ∑ 1/(2^n - 1), and I have vague traces of recent activity on Erdős-type irrationality problems around 2024–25, but nothing I can responsibly cite—I'll flag this as unverified and treat the problem as open per the dossier.
Now I'm working on the conditional Hardy–Littlewood version for the second deliverable, setting up the exact input conditions and using the dossier's Lemma L about runs of length J with logarithmic aftermath.
To verify that L implies irrationality, I'm taking an arbitrary b and s, then choosing J large enough so that the logarithmic bound on the first occurrence N(J) becomes small relative to 2^J/b. The key is that log N(J) = o(2^J) means the prime logarithm A log p_{N(J)} eventually stays below 2^J/b for sufficiently large J, and I need to ensure the run position also satisfies n ≥ s.
The subtlety is that the first occurrence might fall below s, so I need to reformulate L to guarantee infinitely many runs at each scale J with controlled aftermath. For any fixed J, as n grows the aftermath A log p_n eventually exceeds 2^J/b, so I can't just pick any large run—I need simultaneous control over both the height n and the aftermath bound, requiring n to be in some interval [max(s, ...), ...] while keeping A log p_n < 2^J/b. Now I'm formalizing this as Hypothesis L(A): there exists some constant A where for any threshold T, once J is large enough, I can find a run of J consecutive equal values in the gap sequence starting at some position n ≥ T, with the next gap differing by at most A log p_n, and this difference is bounded by 2^J / T. The key insight is that if this hypothesis holds, then S must be irrational, since assuming S is rational leads to a contradiction with the existence of such runs.
I'm simplifying this further by dropping the constant A and stating Hypothesis L* more directly: for any T, there exist J, c, and n ≥ T such that we get J consecutive gaps equal to c, followed by a gap differing from c by something strictly between 0 and 2^J / T. This cleaner formulation captures the same idea—that the gaps can't maintain rational periodicity because the deviations grow in a way that contradicts rationality.
Now I'm connecting this to the main theorem: L* implies S is irrational. This is the minimal hypothesis needed in CPAP form. The next step is relating L* to the Hardy–Littlewood prime-tuples conjecture, which predicts that arithmetic progressions of primes exist with specific densities and first-occurrence patterns. Under that conjecture, we'd expect CPAP-(J+1) progressions with admissible differences (like primorials) to appear around exp(O(J log J)), and the density argument involves balancing the probability of finding such progressions against the probability that no extra primes interfere.
To control the gap δ_{n+J} (the average of subsequent gaps), I need to bound it using a weighted sum of future gaps: δ_{n+J} = Σ g_{n+J+j}2^{-j}. The strategy is to truncate this sum at some point K and bound the tail recursively, so δ_{n+J} ≤ Σ_{j≤K} g 2^{-j} + 2^{-K} δ_{n+J+K}, where the final term is controlled by the fact that all gaps are bounded by p.
Now I'm trying to estimate δ_m using prime gap bounds. The crude approach gives δ_m ≤ Σ_j g_{m+j} 2^{-j}, and I need to bound each gap g_{m+j} in terms of p_{m+j+1}. Bertrand's postulate gives p_{m+j+1} ≤ 2^{j+1} p_m, but this is too weak since the exponential factor cancels the geometric weights. Instead, I should use results from prime number theory—specifically, unconditional bounds on maximal gaps below x, like the Baker–Harman–Pintz result that gives gaps of size O(x^{0.525}). So summing over j gives me O(p_m^{0.525}) unconditionally—that's a solid lemma assuming the Baker–Harman–Pintz bound holds. Without it, the sum diverges, but with any exponent θ around 0.525 or even 21/40, I can get a similar result. ≤ p_m + Σ_{i≤j} g ≤ ... circular. Use p_{m+j} ≤ 2^j p_m only. With ANY θ < 1 unconditional (Hoheisel 1930: θ = 1 − 1/33000; that's classical and certain to exist — Hoheisel proved p_{n+1} − p_n ≪ p_n^{1−δ} for some δ > 0, elementary-to-cite fact, still UNVERIFIED per rules but very safe): δ_m ≤ Σ_j 2^{-j}(2^j p_m)^θ = p_m^θ Σ 2^{-(1−θ)j} = O(p_m^θ) prov
The geometric series converges since θ < 1, giving me an unconditional lemma that δ_m = O_θ(p_m^θ). This bounds the tail error explicitly, though it doesn't help with existence since we'd need J ≳ θ log_2 p_n—runs of length ~ log p are too short. Still, it's a rigorous unconditional result that makes the numerics precise, and it means the map n ↦ δ_n is well-defined with explicit truncation error when computing δ_n from g.
Now I'm working out the error bound when truncating at n+K: the error is O(2^{-K} p_{n+K}^θ) under Hoheisel-type assumptions. For rigorous numerics with only Bertrand's postulate, I need a concrete bound on δ_{n+K}. Using the elementary fact that p_m ≤ 2^m for all m (which follows by induction from Bertrand), I can establish a rigorous upper bound on the tail. I need to shift toward using explicit bounds on prime gaps rather than relying on Bertrand's postulate alone. The Rosser–Schoenfeld bounds give me a concrete, classical result that I can work with numerically: for k ≥ 6, the k-th prime is bounded above by k(ln k + ln ln k), which lets me control the tail sum δ_n through partial summation with verified literature values. The geometric series converges nicely, giving me a tail bound of roughly 2^{-K+1}(n+K)ln(n+K) with exponential decay in K. For the numerical section, I can use Rosser's bound on prime gaps (p_k < k(ln k + ln ln k) for k ≥ 6) to rigorously control the tails, though I'll verify it numerically within my computational range and label any extrapolations carefully. Since I only need moderate values of n and K for the numerics, this approach is manageable.
With K = 50 extra primes, the tail contribution becomes 2^{-50}·δ_{n+50}, which is bounded by roughly 10^6 based on the prime gap bound, giving an error well below 10^{-9}. Now I'm verifying the claim that δ_n averages to log n: the average (1/N)Σ_{n≤N} δ_n decomposes as Σ_j 2^{-j}·(1/N)Σ_n g_{n+j}, which approximates to (p_N/N)·log N using the fact that Σ_{n≤N}g_{n+j} ~ N log N. This checks out. I'm also confirming that δ_n ≥ 2 holds for all n in my range.
For the Lean formulation, since Nat.nth indexes primes starting at 0 with p_0 = 2, the Lean sum becomes Σ_{n≥0} p_{n+1}/2^n = 2S, which matches my definition. The irrationality statement translates directly: 2S is irrational if and only if S is irrational. Now I'm preparing the Lean-ready lemma statements.
I need to sketch out the key theorems: the tail recursion formula, the delta definition, the squeeze identity, the two-window identity, the conditional theorems, and the master criterion. I'll label these as plausible Lean 4 / Mathlib-style statements without claiming they compile.
For the numerical computation, I'm planning to use pure Python with integer arithmetic to compute S as an exact dyadic partial sum up to some cutoff N, avoiding any external dependencies like sympy or mpmath.
Computing the tail bound rigorously: with N around 500 primes (p_500 ≈ 3571), the tail sum of p_n/2^n is bounded by roughly 2^{-500} times a logarithmic factor, giving precision well beyond 147 digits, which matches their reported verification level.
Now I'm computing S to 150 digits and comparing against the continued fraction [3;1,2,13,1,1,2,7,1,2,1,2,18,2,15,...] by extracting the common prefix from rational bounds—that gives a rigorous CF. The identity Σ g_n/2^n = S − 2 is numerically exact by construction, so I'll verify it symbolically instead. I'm also checking whether 9,843,019 is part of a 5-term arithmetic progression of consecutive primes with common difference 30, which would be a known CPAP-5.
Now I'm verifying that 9843019 is indeed the smallest CPAP-5 start by checking the sieve, then computing δ_{n+4} at the last prime 9843139 using the next ~60 gaps to see if |δ − 30| sits near the b=1 threshold around 16.

If the actual value comes out to roughly 13-14, that would contradict the b=1 case, giving me a rigorous computer-assisted proof that S is not a dyadic rational with denominator 2^s for s up to around 650000.
Now I'm checking the prime counting function values: π(10^7) = 664579, so n ≈ 654000, which aligns with my bound.
The key insight is that I've computationally verified S ≠ a/2^s for all integers a and s up to about 6.5×10^5. The continued fraction approach could exclude all denominators up to 2^650000, which is actually stronger than just ruling out dyadic rationals, though both methods require similar computational effort at the π-scale. ined by the CF expansion — I need to verify this classical result carefully. The key insight is that if two numbers have continued fractions agreeing up to a certain depth with large convergent denominators, then any rational in the interval between them must have a larger denominator. This gives me the structural argument: the interval (L, U) with width 10^{-140} contains no rational with denominator ≤ 10^{70}, which combined with the high-precision enclosure establishes that S itself must have denominator exceeding 10^{100}. then I need to verify that if S equals p/q with denominator q ≤ Q, that rational must lie in the interval (L,U). I can use the Stern–Brocot tree approach to find the simplest rational in that interval—the one with the smallest denominator—by computing it directly from the continued fractions of the endpoints. If that simplest rational has denominator greater than Q, I've proven S can't be a ratio of integers with denominator ≤ Q. With 500 digits of precision, this should give me enough power to establish that any rational representation would require a denominator around 10^120 or larger.

For the second approach, I'm looking at the run-threshold computation around the CPAP-5 to get a rigorous bound on δ showing |δ − 30| < 16, which would independently prove S isn't dyadic with s ≤ 654k. But here's the issue: the continued fraction route at 600 digits only covers denominators up to about 10^250, while 2^s with s ≤ 650000 corresponds to a denominator around 10^195000—way outside that range. To actually cover the dyadic case, I'd need to compute S to roughly 650000 bits, which means using all 650k primes and doing massive arithmetic.
Now I'm checking if this is even feasible with Python's big integers. Computing the partial sum via Horner's method—iterating v = 2v + p_n for each prime—means 655k shifts and additions on increasingly large integers. While individual shifts are cheap and each addition is fast, the total complexity is quadratic in N since the integers keep growing, which could take around 10 minutes or longer. That's borderline too slow for this approach.
But actually, the bit-level analysis shows it's more tractable than it first appears. The total work is roughly 7×10^9 word operations, which Python can handle in a few seconds to minutes since the shifts are implemented as efficient C-level memory operations. So it's feasible, though the real win here is that the δ-computation is far cheaper—just handling about 60 gaps plus a tail bound—and it gives a much cleaner pedagogical demonstration of how the squeeze mechanism actually works to exclude values up to 654976.
I'm going to implement both approaches: the continued-fraction route for q > 10^{250} and the squeeze-based dyadic exclusion for s ≤ n₀. The key insight is that the squeeze method needs only those ~60 primes past the run plus a tail bound, whereas the CF approach requires 195000-digit arithmetic to reach a comparable dyadic range. That's a striking quantitative difference—the squeeze converts sparse structural information into denominator exclusions exponentially more efficiently. For the tail bound to be rigorous, I need to verify Rosser's prime bound p_k < k(ln k + ln ln k) numerically across the range I'm working with.
Now I'm hitting a problem with the tail: after 60 gaps, I need bounds for primes beyond my verified range, and I can't verify infinitely many. I'm exploring whether to use a proven inline bound like p_k ≤ 2^k (which turns out to be too loose) or something tighter like p_k ≤ k² for k ≥ 1, which I'd need to derive from Bertrand's postulate. π(2m) ≥ (2m log 2 − log(2m+1))/log(2m), which simplifies to an explicit lower bound of roughly (log 2)·x/log x for x ≥ 2, giving me the subexponential growth I need to control the tail behavior. Now I'm working through the bootstrap argument more carefully: if the k-th prime exceeds 4k ln k, then applying the lower bound on π(p_k) creates a contradiction for sufficiently large k, since the prime counting function would have to be larger than k itself. This forces p_k ≤ 4k ln k for all k beyond some threshold.
For the tail sum, I'm bounding the remaining terms using this bound on the primes, which gives a geometric decay that converges explicitly—the tail is controlled by 4·2^{-K}·(n+K)ln(n+K) times some factor. Bounding the tail sum by factoring out 2^{-K}(n+K+1) and splitting the logarithm terms, then using the fact that Σ2^{-i}(1+i) = 3 to get a clean bound of 2^{-K}(n+K+1)[3ln(n+K+1) + 4], which is rigorous and provable inline. Now I'm verifying the numerical bounds with concrete values: with n around 655,000 and K = 100, the tail bound works out to roughly 10^{-22}, which is comfortably small. I'm also checking the rigorous enclosure for the full sum beyond some cutoff N, using a similar bounding technique that scales with 2^{-N}, and with N = 2000 the result is negligibly small.
For the lower bound, I'm confirming that the tail is strictly positive and bounded below by the first omitted term, giving a valid interval enclosure. I'm also planning to verify the pigeonhole principle numerically by searching for repeated gap patterns in the prime sequence up to 3×10^7, looking for pairs where the 6-gap differences are small but nonzero compared to the threshold 2^6 = 64.
Now I'm working through the contradiction more carefully: if two indices n and m produce a difference |Δ| in their shifted gaps that's positive but less than 2^J/b, then b|Δ| would be strictly between 0 and 2^J, which can't be a multiple of 2^J—this is the key contradiction that rules out certain parameter combinations.
For b=1, any pair with small enough gap at sufficient heights contradicts the bound on s. With b=3 and J=6, I need gaps smaller than about 21, which should be plentiful in the data. Scaling up to J=10 gives even more room. The real power here is that using the two-window method across primes up to 3×10^7, I can systematically eliminate all odd bases up to some threshold B and all shift values up to n₀ by finding the required small-gap pairs for each case.
The key insight is that near-coincidences in the aftermath values become incredibly useful: if two aftermath sequences agree to within 10^{-6}, that tiny difference can exclude bases up to 2^J times larger. With roughly 10^5 pairs of repeated patterns and aftermath values spread across a range of about 40, the birthday paradox suggests the closest pair should have a gap around 40 divided by the square of the number of pairs, giving me access to exclusions for very large bases.
However, I'm realizing that for this specific finite problem, the direct continued fraction approach with 195,000 digits is actually more powerful—it excludes all q up to 10^{195,000}, which dominates what the two-window method can achieve. The two-window machinery's real strength lies in its asymptotic and structural properties rather than in solving this particular instance.
For the numerical validation, I need to be careful about computing differences in δ values. Since each gap carries a tail error of roughly 2·tail(K), I can only claim a meaningful difference |Δ| if it exceeds this error bound. With K = 100 extra primes per site giving tail ~ 10^{-22}, I can distinguish differences larger than about 10^{-21}, which should work for generic near-coincidence pairs.
I should also verify the vacuity claim directly rather than just asserting it. On the overlap question: if a pattern repeats at positions n and m where the windows overlap (m − n = t < J), then the pattern becomes t-periodic on that overlap region. A constant pattern (t = 1 case) would give unconditional CPAP, but the pigeonhole argument doesn't control where repeats actually occur, so overlapping repeats only guarantee periodic patterns, not unconditional ones.
Now I'm wondering if there's a sharper way to apply Zhang-Maynard. The key insight is that Maynard's bound p_{n+1} − p_n ≤ 246 infinitely often immediately gives us some even gap h ≤ 246 that appears as a prime gap infinitely often—this is just pigeonhole on single gaps, the J = 1 case. For J = 2, the bound extends to p_{n+2} − p_n ≤ 398.
The exact constants are a bit fuzzy in my memory—Polymath8b might have 246 and 398, or maybe unconditionally it's something like 6966 for C(2)—but the point is that some pair (h₁, h₂) must repeat infinitely often as consecutive gaps. If h₁ = h₂ we'd get a 3-term arithmetic progression in the primes, which we can't force, but the existence of repeating pairs is solid.
Now I'm thinking about this differently: under rationality assumptions, the fractional parts {δₙ} become eventually periodic with period d = ord_b 2, which means the sequence {2ⁿS} has eventually periodic fractional parts. The integer parts D_n = ⌊δₙ⌋ feed into the gap sequence through the recursion, so I need to track how the periodicity in the fractional parts constrains the gaps.
The key insight is that the gap formula g_{n+1} = 2D_n − D_{n+1} + (2φₙ − φ_{n+1}) simplifies because 2φₙ − φ_{n+1} equals ⌊2φₙ⌋, which is either 0 or 1 (this is just the binary digit from the fractional part). Since the gaps must be even, ⌊2φₙ⌋ must be 0 for n ≥ 2, which forces φₙ < 1/2 for all subsequent terms. So φ_{n+1} = {2φ_n} checks out, and 2φ_n − φ_{n+1} gives me ⌊2φ_n⌋ which is either 0 or 1. The key mistake I made was assuming 2D_n − D_{n+1} was even — it's not necessarily, so g_{n+1} = 2D_n − D_{n+1} + ⌊2φ_n⌋ means D_{n+1} ≡ ⌊2φ_n⌋ (mod 2), not that ⌊2φ_n⌋ must be 0.
This is actually elegant: the parity of ⌊δ_{n+1}⌋ encodes the n-th binary digit of the φ-orbit. No contradiction after all, and it's a good reminder to watch out for these kinds of parity traps.
Now I'm assembling the final deliverables, starting with the claims ledger. C1 establishes that the series S converges and falls in the range (3.6746, 3.6747), with convergence following from the Chebyshev bound on π(x) in C2.
For C2, I need to verify the prime counting bound p_k ≤ 4k ln k holds for all k ≥ 4, checking small cases numerically and then using an asymptotic argument. The core approach uses the binomial inequality (2m)^{π(2m)} ≥ C(2m,m) ≥ 4^m/(2m+1) to derive π(x) ≥ 0.6x/ln x for x ≥ 32, then extends this to the full range. Now I'm working through the contradiction argument for the upper bound on p_k. If p_k exceeds 4k ln k, then since π(p_k) = k by definition, I need to show this leads to a contradiction using the lower bound on π(x) for x ≥ 200, which gives π(x) ≥ 0.6x/ln x. Verifying the bound holds for small cases down to k=2 (p_2 = 3 ≤ 5.5 ✔), establishing C2 rigorously. Then confirming C3 through C5: the gap-sum identity and recursion formulas, the rationality criterion for when δ_n is an integer, and the truncation error bound for computing δ_n from a finite set of gaps. I'm verifying that L* actually implies H-sep by checking whether a run of equal gaps creates the pattern-match condition for consecutive windows, then working through the algebra to confirm the two-window identity holds with the appropriate decay factor. ature input (Maynard 2015), marked as unverified with 0.97 confidence. Under rationality assumptions, the aftermath values at matching positions form a coset in a grid with spacing 2^J/b, so by C9 either finitely many distinct values appear in bounded windows with possible exact coincidences, or they diverge along a subsequence—the key constraint being that distinct aftermath values must be at least 2^J/b apart.
Now examining when two matches have identical aftermath values: this happens exactly when (2^N − 2^M)S is an integer, and under rationality with lowest-terms representation a/(2^s b), this requires M ≥ s and the multiplicative order of 2 modulo b divides N − M. The integer-part matching condition reduces to checking whether (2^N − 2^M)S lands in ℤ.
So if S is irrational, all aftermath values must be distinct—that's the generic case. When S is rational with denominator b and power-of-2 factor 2^s, the differences δ_N − δ_M always lie in (1/b)ℤ for indices N, M ≥ s, and they're integers precisely when the order of 2 mod b divides N − M. This distinctness property is what C8's hypothesis relies on. Now I'm verifying the numerical results: confirming that the smallest denominator exceeds 10^{200}, checking the CPAP-5 entry at 9843019 as the first occurrence with J=4 equal gaps, and computing the exact index n₀ to establish the dyadic exclusion threshold. Then I'll scan for repeated patterns with J=5 through 8 below 3×10^7, analyze the aftermath-difference statistics to find minimal nonzero gaps, and validate all the dossier numerics including the S digit count and continued fraction verification.
For the theoretical components, I'm confirming that the vacuity proposition holds—showing that any finite gap prefix can extend to a rational-consistent tail, which refutes finite-certificate strategies—and then calibrating the Cramér model to predict the distribution of aftermaths and the expected first occurrence of CPAPs, ultimately quantifying how strongly the two-window hypothesis holds under Cramér and independence assumptions.
Now I'm examining how matches of the pigeonhole pattern achieve density far exceeding x/(log x)^{J+1} under the Hardy-Littlewood heuristic, with the aftermath behaving like independent copies of a fixed law, and recognizing that while full aftermath equidistribution can't be proven due to infinitely many gaps, the truncated aftermath—just the first K gaps—forms a finite prime-pattern statement, making H-sep with K-truncated δ^{(K)} provable. Hypothesis HL (exact form): For any threshold T, I need to find gap patterns h and h′ that share a common prefix up to position J, both realized by consecutive primes at heights above T, where the weighted deviation between their tails (positions J+1 through J+K) falls into a specific narrow range controlled by K and T—this pins down the exact uniformity requirement for the Hardy–Littlewood reduction.
Now I'm refining the height constraints: the differences δ_{n+J} and δ_{m+J} must be nonzero but smaller than 2^J/T, and they decompose into the weighted tail deviation η plus a remainder term scaled by 2^{−K}. To make this work with the C5 bounds on δ values, I need to restrict both heights n and m to stay below some bound X, which forces η to satisfy a sandwich condition: it must be large enough to dominate the logarithmic error term Λ(X) (which grows with the prime-counting function and K), but small enough that adding this error still keeps the total below 2^J/T. I'm working through the sum more carefully now, bounding each term by factoring out what I can and using logarithm properties to combine them. The calculation shows the bound grows linearly with n, which seems worse than expected—it should be closer to logarithmic—but this appears to be what the current approach yields.
Now I'm reconsidering the gap between consecutive primes differently: instead of just the upper bound, I need to use both an upper and lower bound on the prime counting function to get a tighter estimate. Rosser's theorem gives a classical lower bound on individual primes that might help here, though proving it with elementary constants inline isn't straightforward. I'm also thinking about whether Chebyshev's bounds on the prime theta function could provide the lower bound I need. So the lower bound on p_k gives roughly k ln k, but when I try to use this to bound the gap g_{n+j}, the linear growth in n and j dominates—the difference between consecutive primes stays on the order of (n+j) ln(n+j), which doesn't improve over what we already know. The unconditional best results come from deeper analytic techniques like the Hoheisel bound or Baker-Harman-Pintz, which achieve exponents around 0.525 or better.
With only elementary methods, I'm stuck with Bertrand-type bounds where g_n < p_n, giving roughly O(n log n) gaps. But when I sum the weighted gaps for the tail—using 2^{-j} weights to control the series—I can show that δ_n ≤ C n ln n, which is manageable for numerical purposes even if it doesn't break the elementary barrier. The uniform Hardy-Littlewood conjecture allows tuples up to roughly log x / log log x, but I'm running into a critical issue: the gap pattern I need (K ≈ log₂ X) actually exceeds this bound for large X, which means the expected count of primes drops to X/(log X)^{log₂ X}—essentially vanishing as the exponent grows logarithmically with X itself.
But wait, I don't need to prescribe all the gaps exactly; I'm looking for pairs of positions (n,m ≤ X) that share a prefix match in their gap sequences up to some length J, then differ in a controlled way in the tail gaps—so I should recount by allowing freedom over many possible patterns rather than fixing them all. The aftermath differences are smooth random variables on the scale of log X, so the probability of them differing by less than ε is roughly ε/log X, which still leaves plenty of pairs—on the order of X^{2−o(1)}·ε/log X. To verify condition (b) from finite data, I only need the two aftermaths to diverge in their first K weighted gaps by more than the tail uncertainty, where K scales like log₂(X) + log₂(1/ε), but I don't need to specify those gaps in advance—they're determined by wherever the primes fall. The hypothesis just asserts that such a pair exists satisfying both conditions.
To formalize this as a standard hypothesis, I'd sum over pattern pairs using a second-moment type of Hardy-Littlewood statement: the key input is a lower bound on the pattern-repetition second moment Σ_h N(h)² plus equidistribution of the aftermath functional at matched pairs—this is exactly what Gallagher proved in 1976 for fixed k under uniform Hardy-Littlewood. For fixed J and aftermath windows of fixed length K, the needed precision scales like 2^J/T, so K grows roughly as J + log₂ T + log₂(δ-scale), where the δ-scale itself grows logarithmically. I can use Chebyshev's inequality to bound the average deviation unconditionally—roughly half the values stay within 2 log X—and I'm working through the telescoping sum argument to show that the total deviation across all n ≤ N is bounded by the prime counting function, which grows like N ln N. n+J+K} ≤ 25 ln X to keep a positive proportion of candidates. For the final exact Heilbronn-Linnik formulation, I'm setting parameters T and deriving J and K logarithmically in terms of T and X, then constructing a hypothesis that two primes in the range [T, π(X)] share a J-prefix match with both having truncated aftermath in the good range and gap difference bounded appropriately, though I need to ensure the δ bounds stay finite-certificate rather than relying on prime properties.
I'm now deciding between two approaches: state the hypothesis cleanly in terms of δ (which is intrinsic to the primes) as the primary version, then provide a finite-pattern-checkable variant using Markov's bound to convert the tail control. The key insight is that Markov gives a bound for "most n" rather than specific n, so I can demand the pair lies in the good half of the range, keeping each ingredient standard while still being prime-intrinsic, and I'll note exactly where the uniform Heilbronn-Linnik bound enters through pair-correlation. The key constraint is equidistribution of gap differences between prefix-matched pairs, with anti-concentration near zero, where the gap parameter K grows logarithmically with X. This sharpens the earlier framing by replacing the need for unconditional CPAP existence with a more targeted pair-correlation hypothesis, trading one conjectural input for another that's potentially more tractable. So the real refutation needs to be sharper: for any fixed J and b, the squeeze criterion fails for all but finitely many runs when aftermaths diverge to infinity — but that's just a conditional statement, not a true refutation. The actual target is to show that growth and monotonicity arguments alone can't work, which means proving the map from sequences to the sum isn't injective across different growth classes. I can also construct a tighter counterexample using the calibration sum, finding a sequence with growth like n log n that looks admissible under the even-gap conditions but still breaks the framework.
The key insight is that I can greedily steer the gaps within allowed windows to hit a specific rational value for the tail sum, since the gap choices give enough flexibility while keeping the growth rate intact. This means any proof that works has to rely on something deeper about primes than just their growth rate, gap structure, and counting asymptotics — it needs finer prime patterns. That's a concrete, provable obstruction I can demonstrate. I'm working through the binary steering mechanism to keep the target rational value within the achievable window at each step, using the greedy choice of ε_j to maintain the invariant that the remainder stays in [0, 2^{1−j}], then setting the target as the ceiling of the base sum plus one to ensure it's rational and within bounds.
The resulting gap sequence h_j follows the pattern ln j + O(1), which gives the total cost q_n ~ n ln n as required, matching the average behavior of prime gaps.
Now I'm verifying that the counting function Q(x) = #{q_n ≤ x} follows x/ln x(1+o(1)), which is the prime number theorem analogue. This confirms the key result: a rational sequence can be constructed that matches all the structural properties—PNT-type asymptotics, even gaps, and gap-size regularity—yet remains rational. This is sharper than the original calibration and shows that no finite set of linear statistics can distinguish this rational sequence from the irrationals, since I can freely choose the binary digits on sparse sets to satisfy any congruence constraints.
The same greedy construction works with non-dyadic rationals too: any τ = odd/b is achievable because the binary steering with increments of 2^{1−j} covers all reals in the target interval, including all rationals. I should also verify the coset claim for C10: checking that the difference δ_n − δ_m satisfies the scaling relation b(δ_n − δ_m) ∈ ℤ and the recursive structure. The proof works by showing that if the expansion were rational with denominator 2^s b in lowest terms, then for T = max(s,b), any matching patterns at positions n,m ≥ T would force b times the difference of the δ values to be a nonzero multiple of 2^J, but this leads to a contradiction when comparing the bounds. Theorem A follows as a special case through the overlap structure, though I should also present its standalone proof from the original dossier since it's cleaner that way. Now I'm considering whether to weaken Theorem B's hypothesis further by dropping the exact pattern equality requirement and instead using a condition about weighted prefix differences being dyadically negligible. (δ_n − δ_m), ℤ) > 0 means the difference isn't in the lattice (1/b)ℤ. The master criterion simplifies to: for any b, there exists some n where bδ_n isn't an integer—this is what the certificate schemes (runs, two-windows) are proving. I'm wondering if there are other efficient certificate constructions beyond these, like using three-term differences.
I should also note the entropy angle: the irrationality of S connects to whether weighted sums of the fractional parts {δ_n} eventually become periodic mod 1, which ties to positive-entropy statements about the carry chain. The key insight is that {δ_n} must take at least two values infinitely often outside any lattice (1/b)ℤ.
For the numerical implementation, I'm planning a straightforward bash and Python approach without external dependencies—starting with computing the SHA256 hash of the uploaded file as the user requested.
Sieving primes up to 3×10^7 using a bytearray...
Computing the exact partial sum T for the first 2000 primes using Horner's method with big integers, then establishing a rigorous tail bound. I'm working through the algebra to show that the tail sum is bounded by 2^{-N}(N+1)ln(N+1) times a convergent series, which simplifies to a crude but proven bracket of 2^{-N}(N+1)[3ln(N+1) + 8]. So the tail bound works out to 4·2^{-N}(N+1)(2 ln(N+1) + 4), which I can verify using the inequalities p_k ≤ 4k ln k and ln i ≤ i−1 along with the computed sums. For N = 2000, this gives an enclosure width around 10^{-597}.
Now I'm computing the continued fraction representation by treating the bounds as exact rationals L = T/2^N and U = (T + ceil-bound)/2^N, then finding their common CF prefix to extract the guaranteed digits. I'll use the Stern–Brocot interval algorithm to iteratively identify matching floor values and emit CF terms until the bounds diverge. Now I need to verify that any rational equal to S has a denominator exceeding a threshold Q₀ around 10^29, then locate all maximal equal-gap runs of length at least 3 below 3×10^7, confirming the longest run has J=4 (a 5-term arithmetic progression in primes) with its first occurrence at 9843019, and finally compute δ values at the boundaries of these runs. —completing the tail bound calculation with the derived formula, plugging in m ≈ 6.6e5 and K = 120 to verify the error is around 10^{-28}, which confirms the bound is tight enough for computing δ_{n+4}.
Now I need to verify that S is never a dyadic rational by checking that δ_n ∈ ℤ leads to a contradiction for all s ≤ n₀, where n₀ is determined by computing π(9843019) exactly from a sieve. Then I'll run a two-window pattern search across prime gaps up to π(3e7) − 200, looking for repeated hash patterns in windows of size 6 and computing the differences between matching occurrences. Since a single pair with the smallest observed gap works for all b values below the threshold, I can set B₀ = ⌊64/Δ_min⌋ and check pattern repeats with J = 10 to find larger thresholds, examining whether length-10 gap windows cluster or repeat within the 3e7 range.
For the overlap concern: even if two pattern occurrences overlap (m ≤ n+J), the algebraic identity δ_n − δ_m = 2^{-J}(δ_{n+J} − δ_{m+J}) still holds because it follows directly from the prefix equality, so overlapping patterns don't break correctness.
Now I'm setting up numerical sanity checks: verifying the recursion δ_{n+1} = 2δ_n − g_{n+1} against computed values, confirming the sum Σg/2^n equals S − 2 to working precision, and spot-checking that the fractional parts {2^nS} match {δ_n}. I'll also optionally verify the Rosser bound p_k ≤ 4k ln k for the prime indices.
For the k ≥ 12 threshold, I need to verify numerically that π(x) ≥ 0.6x/ln x holds across the range 200 to 3×10^7 using a sieve, which serves as a sanity check for the binomial argument proof that establishes this bound for all x ≥ 200. Now I'm deriving the lower bound for π(x) by setting m = ⌊x/2⌋ and working through the inequality, being careful with the logarithmic terms and their direction to ensure the bound holds for all x ≥ 2. x ≥ 10, so that inequality holds. Now I'm checking whether p_k ≤ 4k ln k for k ≥ 12, which requires finding when 4k ln k ≥ 200 to apply the earlier bound—this happens around k = 18. Then I'm computing the prime counting function at 4k ln k to establish the upper bound on p_k.
For k = 18 and beyond, I'm verifying that 1.4 ln k − ln ln k exceeds 1.387, which ensures π(4k ln k) > k, confirming the inequality holds. Since this expression is increasing for k ≥ 3, the bound extends to all k ≥ 18, and I've checked the remaining cases k = 2 through 17 numerically to complete the proof.
Now I'm handling the constraint that gaps appear only for indices ≥ 2, so pattern matching and vacuity arguments start from n ≥ 2. For the converse of C4, if some integer b times δ_n is an integer, then δ_n is rational, making u_n rational, which means S itself becomes rational.
For the average behavior of δ_n, I'm using the telescoping sum representation and applying the Prime Number Theorem along with Chebyshev bounds to sandwich the average between c₁ ln N and C₂ ln N for large N, establishing that δ_n grows logarithmically on average.
To make this rigorous with explicit constants, I need to prove the lower bound on p_k inline: starting from the binomial coefficient bound θ(2m) − θ(m) ≤ m ln 4 (since each prime p in that range divides C(2m,m) exactly once), then iterating this dyadic argument to show θ(x) ≤ x ln 4 unconditionally, which feeds back into the prime gap average estimate. for p_k ≥ 100, I can verify that the √p_k term becomes negligible compared to p_k/ln p_k, so the bound simplifies and the recursion holds. Verifying the bounds with both Chebyshev inequalities and finite numerical checks, so the constants are now fully justified without external dependencies.


--- REACHED TURN LIMIT ---

Operator: Weiter

The user wants me to compute the SHA256 sum of an attached file and present the result as a Markdown file. They've prompted me to continue with "Weiter," so I need to proceed with calculating the hash now.

Now I'm working through the mathematical task: Erdős Problem #251 about whether the sum of prime numbers divided by powers of 2 is irrational. This is a famous open problem, so I'll make rigorous partial progress rather than claiming a solution. I'm setting up the core definitions—S as the sum of p_n/2^n where p_n is the nth prime—and verifying key relationships like how the gap sequence g_n relates to S, then working toward establishing what I can prove rigorously and where the difficulty lies. I'm working through the algebra to show that delta_n equals the weighted average of future gaps, where each gap g_{n+j} is weighted by 2^{-j}, and these weights sum to 1.
Since all gaps except g_1 are at least 2, this means delta_n >= 2 for n >= 1. I can also derive a recursion relation: delta_{n+1} = 2*delta_n - g_{n+1}, which follows from the weighted sum structure.
Now I'm moving toward establishing a rationality criterion for when S equals a ratio of powers of 2. n - b p{n+1} ∈ ℤ. And conversely, if b delta_n is an integer for just one value of n with b odd, then u_n becomes rational with an odd denominator dividing b, making S rational as well. For the squeeze case with J consecutive equal gaps c starting after index n, I'm verifying that delta_n - c relates to delta_{n+J} - c by a factor of 2^{-J}, which follows from repeatedly applying the recurrence delta_{n+1} = 2(delta_n - c).
Now I'm analyzing what happens when b delta_n is an integer and c is an integer: the quantity b(delta_n - c) must also be an integer, so its absolute value is at least 1 unless delta_n equals c exactly. This means |delta_{n+J} - c| grows by a factor of 2^J, giving a lower bound of 2^J/b unless delta_n = c. The key tension is whether I can show 0 < |delta_{n+J} - c| < 2^J/b to reach a contradiction, but delta_n = c exactly would require all future gaps to average to c, which seems like a special case worth examining more carefully. The aftermath size scales with log p_n, so I need runs long enough that 2^J/b exceeds this, meaning J must grow roughly as log₂(b log p_n). At the height where the first such run appears, N(J) grows like exp(J log J) by Cramér's conjecture, giving log N(J) ~ J log J, which is vastly smaller than 2^J—plenty of room. Though unconditionally proving infinitely many equal consecutive gaps remains open, even for the simplest case.
Now I'm thinking about what I can actually contribute here. The dossier already sketches a conditional approach: state a precise hypothesis and prove it implies S is irrational. I should formalize that into a rigorous partial result.
The key is to carefully state hypothesis (L): for every J ≥ 1, there exist infinitely many positions where we have a run of J equal gaps (meaning J+1 consecutive primes in arithmetic progression), with controlled aftermath—the deviation from the common gap stays bounded by C log p_n. But I need to be careful about how to quantify this for each fixed gap size b, and handle the relationship between the run length and the bounds correctly. some m implies S is a dyadic rational, which is a special case we can handle separately. Let me formalize the conditional theorem: assuming S is rational with S = a/(2^s b) where b is odd, then b·delta_n is an integer for all n >= s. For a specific J where 2^J exceeds b times the supremum of the aftermath at available heights, I need to find some n >= s and a run length J with equal gaps c on the interval (n, n+J] such that the difference between delta_{n+J} and c stays strictly between 0 and 2^J/b.
Now if b times (delta_n - c) equals 2^{-J} times b times (delta_{n+J} - c), this product lies strictly between -1 and 1 in absolute value, yet it must be an integer since both b·delta_n and b·c are integers — a contradiction. The key insight is that delta_n - c cannot equal zero precisely because delta_{n+J} - c is nonzero.
So the correct hypothesis should be: for every real B >= 1, I can find arbitrarily large n along with some J >= 1 and even c such that the gaps g_{n+1} through g_{n+J} all equal c, and the absolute difference between delta_{n+J} and c stays at most 2^J/B.
Now I need to handle whether this difference can actually be zero. If delta_m equals some integer c for some index m, then S would factor as a ratio involving 2^m, making it a dyadic rational. But I'm already assuming S is rational for the sake of deriving a contradiction, so the theorem really states that hypothesis (L) implies S must be irrational. Let me set up the proof cleanly: assume S is rational with an odd denominator part b and dyadic exponent s, then apply (L) with B = b and starting point n_0 = s to obtain n >= s, and proceed from there. ℤ for all m, or every aftermath stays nonzero. So I could weaken condition (L) by separately ruling out S being dyadic rational—if S = a/2^s, then delta_n would be an integer for all sufficiently large n, and integrality would propagate through the recurrence without creating a contradiction. But I'm wondering if there's any constraint from the size of delta_n that could rule out the dyadic case unconditionally.
The key observation is that delta_s must be an integer, and then delta_{n+1} = 2delta_n - g_{n+1} automatically stays integral. So it's really just one condition to check. The question becomes whether a weighted average of future gaps can actually equal an integer—there's no obvious contradiction there. With b=1, the squeeze argument requires finding J such that 0 < |delta_{n+J} - c| < 2^J, which means J needs to exceed log₂ of the aftermath size.
Looking at the numerics, when p ≈ 9.8e6, we get log₂ log p ≈ 4.01, right at the threshold with J=4. So even the b=1 case remains open but sits at the boundary of what current numerics can handle—ruling out dyadic rationals is essentially unprovable with present methods.
For the deliverable on the conditional theorem, I can state the criterion precisely and prove it rigorously implies irrationality. The dossier sketches this already, but I can add careful quantifiers, a complete proof, and maybe a cleaner equivalent form of the criterion plus new reductions. Now I'm thinking about characterizing when S is rational in terms of the existence of some parameter b satisfying certain properties. ℤ ⟺ the fractional parts x_n = frac(delta_n) form an orbit under the doubling map T(x) = 2x mod 1, evolving independently of the gap sequence. So the rationality of S depends entirely on whether this orbit is eventually periodic. So the recursion checks out: delta_1 = 2delta_0 - g_1, which means x_n = {2^nS} under the doubling map. This is the key insight—the orbit's eventual periodicity directly reflects whether S's binary expansion is eventually periodic, which happens precisely when S is rational.
Now I'm thinking about elimination schemes over consecutive values of n, since the problem explicitly suggests this approach. We have the relation b*delta_n... The Maynard-Tao result on bounded gaps between clusters of primes is key here: infinitely often we get m+1 consecutive primes packed into a window of size C(m), which constrains the weighted average delta_n. Since delta_n is a sum of future gaps weighted by powers of 1/2, a cluster of bounded gaps early on keeps delta_n small, though the tail contribution from gaps beyond the cluster could still be large.
The challenge is that C(m) grows exponentially with m (roughly e^{3.8m} or so), so even with m ~ log log p_n, the bound delta_n <= C(m) + 2^{-m} log p_n doesn't quite force delta_n to be small enough to break through the squeeze. Clusters give us small gaps, but not the equal gaps we'd need to make the argument work.
I'm wondering if there's a way to use clusters differently—maybe to force a contradiction through the rationality constraint. Under rationality, delta_n must be in (1/b)ℤ and at least 2, so clusters making delta_n small doesn't immediately create a contradiction since integers >= 2 still exist. And since (1/b)ℤ is dense, there's no gap-free interval to exploit either.
What if I relax the equal-gap requirement? Instead of needing delta_n - c to double exactly, I could allow a sequence of gaps g_{n+1}, ..., g_{n+J} to take values close to c + e for some small e, and see if that two-scale squeeze still works. For the contradiction to hold, I need |delta_{n+J} - c + E| to stay within bounds, but E is heavily weighted toward early errors—the first gap deviation e_1 carries weight 2^{J-1}, making it catastrophic, while later deviations e_j near J only have weight 2^{J-j} and are tolerable. This means the run must start with exactly equal gaps and can only tolerate small deviations toward the end, which weakens the requirement from needing J+1 primes in perfect arithmetic progression to something slightly more flexible.
I could also reverse the recursion: running backward through the doubling map, errors get halved at each step, but since integrality holds at all n ≥ s, there's no asymmetry to exploit there. Another angle might be to look at digit or entropy statistics instead.
Now I'm thinking about the orbit under the doubling map: if x_n = {2^n S} is eventually periodic (meaning S is rational), then the fractional parts of delta_n would take only finitely many values. The question becomes whether the distribution of frac(delta_n) provably avoids concentrating on finitely many rationals—and unconditional equidistribution of delta_n would rule that out entirely. Now I'm crystallizing this as a clean equivalence: S is rational if and only if the fractional parts of the gaps take finitely many values, which happens if and only if the sequence becomes eventually periodic. This gives me a concrete numerical approach — I can try to rule out small-denominator rationals by computing the continued fraction expansion from the prime gaps up to around 3×10^7, which gives me roughly 1.86 million primes to work with.
The key insight is that if S were a dyadic rational of the form a/(2^s b), the periodicity would eventually show up in the gap sequence, but I'd need to know the denominator to verify this. However, I can extract something unconditional from the partial quotients themselves — specifically, looking at that run around p = 9,843,019 where the squeeze condition holds tightly at the threshold, the gap deviations stay bounded by powers of 2 in a way that constrains what the irrationality measure could be. Right, any dyadic rational a/2^s has delta_n as an integer for all n >= s, so if s <= n* ≈ 650k, I'd get a contradiction from the numerics. This means S isn't a dyadic rational with terminating binary expansion up to that point. But direct computation of the binary digits to 1.8 million places already rules out any eventually-periodic pattern anyway, so the squeeze argument doesn't add anything new here.
Now I'm being more careful about what the numerics actually certify: computing S's digits requires the full tail sum, and partial sums only pin down digits up to the error threshold. With primes up to 3×10^7, the tail u_N/2^N is incredibly small (around 2^{-1.8M}), but the tail's contribution scales as 2^{-N}(p_{N+1}+...), which affects digits around position N - log₂(p_N). So I can confidently certify roughly the first 1.8 million minus 25 digits.
Carry propagation from the tail is generically not an issue — it would only matter if there were long blocks of all 1s in the binary expansion, which is unlikely. So ruling out dyadic rationals with s ≤ 10^6 is certifiable in principle, though it's a minor point.
The more interesting question is whether I can rule out b=1 entirely — meaning S is genuinely not dyadic for any s. That would require infinitely many digit changes in the binary expansion, i.e., the expansion never becomes eventually constant. If S were exactly a/2^s, then the tail would have to vanish, but all terms are positive so that's impossible. The key insight is that 2^s S ∈ ℤ would require u_s = 2^s(S - S_s) to equal zero, which can't happen.
Now I'm checking whether the delta_n sequence being integer-valued for all n ≥ s leads to a contradiction. If delta_n ∈ ℤ for all n ≥ s, then the recurrence g_{n+1} = 2 delta_n - delta_{n+1} doesn't automatically violate any constraints. But maybe I can derive a contradiction by looking at where record small clusters occur in the gap sequence, using results from Maynard-Tao theory.
Actually, let me examine the parity structure more carefully. Since all gaps are even, if delta_n ∈ ℤ for all n ≥ s, then g_{n+1} ≡ delta_{n+1} (mod 2) forces all delta values to be even integers. This is consistent with the recurrence, but it also means delta_{n+1} ≡ g_{n+1} (mod 4), which might impose stronger divisibility constraints that eventually lead to a contradiction. So the recursion actually establishes that delta_s is an integer if and only if delta_n is an integer for all n ≥ s — it's really just one condition encoded across the sequence. The key insight is that given any tail of gaps, delta_s is completely determined; asking whether it's an integer is a single real equation with no hidden congruence constraints on the gaps themselves. This means unconditional results about prime gaps would need actual information about gap behavior, not just abstract consistency. The question of whether consecutive gaps are equal infinitely often remains open, as does whether their differences stay bounded infinitely often — these are genuinely hard problems in the theory of prime gaps. infinities hold, but equality infinitely often remains open unconditionally. Three consecutive primes in arithmetic progression is another open problem — there are conditional results via Hardy-Littlewood and Balog, but I should mark this as unverified. The unconditional squeeze approach hits a wall immediately: even at J=1 (a single gap), I need to bound the deviation from some constant c, which requires 0 < |delta_{n+1} - c| < 2/b.
For b=1, this becomes |delta_{n+1} - g_{n+1}| < 2, meaning the weighted average of future gaps must stay within 2 of the current gap. If g_{n+1} is a twin prime gap (2) and delta_{n+1} < 4, I'd need clusters of small gaps concentrated together — something Maynard-Tao gives us for large m, but I'm not sure this directly yields what I need.
Now I'm working through the weighted sum bound: if I pack many primes with gaps g_{n+1},...,g_{n+m} summing to at most H_m, then delta_{n+1} is bounded by H_m plus a decaying tail term involving delta_{n+m}. The challenge is bounding delta_k itself — using the Baker-Harman-Pintz bound on gaps (g_m ≪ p_m^{0.525}) and approximating p_{k+j} in terms of p_k, I'm trying to get a crude upper bound on delta_k as a sum of weighted gaps.
The naive approach using Bertrand's postulate (p_{m+1} < 2p_m) gives p_{k+j} < 2^j p_k, which makes the weighted sum diverge, so I need something tighter. Instead, I'm bootstrapping with the BHP bound: p_{k+j} ≤ p_k + C times a sum of fractional powers of earlier primes, which simplifies to p_{k+j} ≤ p_k + C' j p_k^{0.525} for j up to around p_k^{0.4}.
Beyond that range, I can use Chebyshev's theorem (p_m ≍ m log m) unconditionally. The key insight is that the weighted gap sum delta_k = Σ_j g_{k+j} 2^{-j} stays bounded by p_k^{0.525} or even p_k itself when I account for the exponential decay of the weights, so the contribution from all gaps stays manageable.
Now I'm trying to bound delta_{n+1} by combining this with a truncation at level m. If I choose m ~ 2 log₂ p_n, the tail 2^{-m} p_n vanishes, leaving delta_{n+1} ≤ H_m + o(1). But here's the problem: H_m (the sum of the first m harmonic-like terms) grows like m e^{4m}, which explodes exponentially and completely dominates the bound. For this approach to work, I'd need H_m to be much smaller than 2^m, which would require finding m consecutive primes packed into an interval of length 2^m — a statement that's weaker than what Maynard proved but still not obviously true for all large m.
The real issue is that Maynard's bound gives H_m ≪ e^{cm} with c ≈ 3.8, which is vastly larger than log 2 ≈ 0.693. So the prime clusters Maynard found are too sparse — the gaps are too wide — to make the geometric weighting strategy work. This is a concrete quantitative obstruction worth documenting as a rigorous refutation of what seemed like a natural approach.
To actually force δ_n = O(1) through clusters, you'd need m primes packed into intervals of length o(2^m), meaning H_m = o(2^m). That's currently unknown — we only know H_m ≥ (1+o(1))m log m trivially, and the Hardy-Littlewood conjecture suggests H_m ≍ m log m, which would be fine in principle but isn't proven. So the cluster route to small delta requires H_m ≪ 2^m, which remains open while Maynard gives e^{3.8m}.
Now I'm checking what small delta would actually accomplish — if δ_n < 4 and we want g_{n+1} = 2 with the J=1 squeeze at b=1, we need |δ_{n+1} - 2| < 2, placing δ_{n+1} in (0,4) excluding 2. So for the dyadic case, I need infinitely many twin clusters where consecutive gaps equal 2, creating a recursive constraint: if g_{n+1} = 2, then delta_n = 1 + delta_{n+1}/2, which forces delta_{n+1} < 6 to keep delta_n below 4. This cascades downward, essentially requiring arbitrarily long blocks of gaps all equal to 2 to maintain the bound.
The real obstacle is that even achieving b = 1 (where S isn't dyadic rational) depends on having infinitely many places with small consecutive gaps — something tied to deep open questions like whether liminf g_n ≤ 6, which remains unknown despite knowing liminf g_n ≤ 246 from Polymath8b. So this becomes a striking calibration point: the dyadic case hinges on twin-prime-like clustering that's genuinely unconditional. So if delta_n is an integer for even one value of n, then it propagates forward to all subsequent values, meaning S would be dyadic. To rule that out completely, I need delta_n to be non-integer for every n in the orbit — that's a universal quantifier over infinitely many terms, which is hard to establish without additional structure. Let me explore a different angle: maybe I can use two different scales or interleave them, or connect S to base-2 digit properties and known results on specific numbers. There's literature on series of the form p_n/2^n and irrationality results that might be relevant here.
I'm recalling that Erdős posed the question of whether sum p_n/2^n is irrational, and there are related results on sums like 1/(2^{p_n}). There are also classical Erdős-type criteria: if a sequence grows fast enough, then sum a_n/2^n is irrational. For instance, Erdős proved in 1948 that sum d(n)/2^n is irrational, where d is the divisor function, using the strong fluctuation properties of d. Similar results exist for sum σ(n)/2^n. But for primes specifically, the question remains open.
I'm noting there's a theorem (possibly Borwein) on sums like 1/(2^n - 1) being irrational, though it's not directly applicable here. I'll keep any literature references minimal and mark them as unverified.
For Idea 6, I'm exploring an elimination scheme over consecutive n values. Under the rationality assumption, I'd have m_n = b·delta_n as an integer, with m_{n+1} = 2m_n - b g_{n+1}, where delta_n stays bounded by something logarithmic on average. Computing the average of delta_n over n ≤ N by summing over gaps with appropriate weights gives roughly p_N ≈ N log N, so the average delta scales like log N.
Now I'm considering a direct approach: can I trap delta_n in an open interval (k, k+1) with certainty for some n? If so, that would force delta_n to be non-integral and refute b=1 up to that point—though this only gives finite information. Trapping delta_n in (2,3) unconditionally would require knowing future gaps, which isn't feasible. I'm also thinking about leveraging the full orbit constraint: x_n = {2^n S} forms a doubling orbit, and rationality would mean a finite orbit, but I need to work out how delta_n relates to this under the rationality assumption.
Actually, here's something stronger: if the denominator is q = 2^s b, then eventually x_n cycles through values in {j/b} with period t = ord_b(2). This means x_{n+t} = x_n for large enough n, which translates to frac(delta_{n+t}) = frac(delta_n)—so delta_{n+t} - delta_n must be an integer for all sufficiently large n. The gap-difference sum over the shift by t is integer-rigid, which looks like a powerful constraint that might force a contradiction.
Now I'm exploring whether this periodicity condition actually holds. The shift-difference y_n = delta_{n+t} - delta_n is itself an integer-valued sequence that can be expressed as a weighted average of the gap-differences at lag t, and it evolves according to a recurrence relation involving the gaps themselves.
The key insight is to look at sign changes in this sequence. Since y_n must always be an integer, if it changes sign between consecutive terms, that creates a constraint. When y_n = 0, the recurrence forces y_{n+1} to equal the negative of a specific gap-difference, which is always even. This suggests examining what happens when the gaps satisfy certain divisibility properties. Under rationality constraints, delta_n lives on a lattice and stays bounded between 2 and large values while fluctuating. The honest assessment is that an unconditional result is out of reach, but I can deliver several rigorous results: a fully conditional theorem with exact hypotheses, proved equivalences like the doubling-map connection and the rationality criterion, and a generalized squeeze argument with error terms showing why exact equality at the start is necessary. I can also rigorously rule out the cluster approach since the gap bounds grow much faster than the cluster size. To make the bound work, I need the weighted gap sum plus a tail term to stay below some constant C. This requires roughly log₂(p_n) primes in a window, and the window length scales exponentially with that—specifically, H grows like e^{3.8 log₂ p}, which translates to a relationship between H and p_n through the exponential.
But wait, that gives H ~ p^{5.48}, which is absurdly large—longer than p itself. If I instead assume the conjectural bound H_r ~ r log r, the window becomes much tighter, scaling like log p · loglog p. Yet that's not actually small; it's the typical size of gaps themselves, so I'm not gaining anything.
I realize I've been conflating two different goals. For the J=1, b=1 case, I genuinely need delta_{n+1} < 4—extremely small, which demands prime tuples with gaps of 2 or so. But for general b, I don't need smallness at all; I need proximity to an integer within 1/b. The run-squeeze achieves this through doubling amplification, and I don't see an alternative route to get that proximity.
Let me verify the equivalence claim: rationality holds if and only if there exists b such that b·delta_n is an integer for some n. The forward direction is done. For the converse, if b·delta_n is an integer, then u_n = p_{n+1} + delta_n satisfies b·u_n ∈ ℤ, which means S = S_n + u_n·2^{-n} is rational with denominator whose odd part divides b and dyadic part at most n. That checks out.
Now I'm formulating the conditional theorem with precise hypotheses in two variants. Variant A uses equal-gap runs or CPAP form: the hypothesis states that for every positive integer B, there exist n ≥ B, J ≥ 1, and an even c ≥ 2 such that... So completing the contradiction: that integer is nonzero but has absolute value less than 1, which is impossible. Now I'm shifting to connect this result to a more standard hypothesis involving a constant C where for infinitely many n, the gaps stay bounded by C log p_n while maintaining the required nonzero separation condition.
If delta ever lands on an integer, then S becomes dyadic rational, which I can handle separately—applying the result with B = max(s,1) directly contradicts the existence of any rational S satisfying these conditions.
Now I'm checking whether (L') with Cramér's conjecture implies (R). For a given B, I need to find runs of length J where the logarithmic gap stays bounded by C log p_n, which requires that such J-runs first appear at heights where log p_n is small relative to 2^J. The key constraints are that infinitely many J-runs must exist with the right gap properties, and their first occurrence must happen early enough. The issue is that N(J) might stay bounded while J grows, so I need to enforce that the indices themselves go to infinity—perhaps by conditioning on n₀ and looking at the liminf over J of runs at positions n ≥ n₀. Rather than keep refining this technical detail, it's cleaner to just state (R) as the core hypothesis and note that Cramér and Hardy-Littlewood heuristics support it overwhelmingly. I should also ask whether a quantitative Hardy-Littlewood prime tuple conjecture with uniformity could actually prove (R), and if so, what the exact uniform statement would be.
Now I'm trying to derive (R) from quantitative Hardy-Littlewood by analyzing what's needed for a J-run of gap c: I need J+1 consecutive primes spaced exactly c apart with no primes in between, which is a prime tuple with specific gap constraints. Hardy-Littlewood predicts roughly 𝔖 x / (log x)^{J+1} such tuples, and the intermediate compositeness is generically satisfied. The tuple 0, c, 2c, ..., Jc is admissible only if c avoids small prime divisors in a certain way.
For admissibility, I need to check each prime q ≤ J+1: if q divides c, all tuple points are congruent to p mod q (just one residue class), which is fine. If q doesn't divide c and q ≤ J, then as j ranges from 0 to J, the residues jc mod q cover multiple classes, potentially hitting all of them, which would violate admissibility.
So c must be divisible by all primes q ≤ J — essentially a primorial. This means c ≥ J# ≈ e^J, which matches the empirical data: the J=4 run uses c = 30 = 2·3·5, and smaller J values require c divisible by the appropriate prime products. So for J=2 we get 6 | c, which matches the classical CPAP-3 result. Now I'm working through the heuristic model: under the assumption of uniform distribution in J, the expected count of J-runs up to x follows the Hardy-Littlewood prediction scaled by the singular series and logarithmic factors. To find the first occurrence x_J, I need the expected count to reach 1, which gives x_J roughly proportional to (log x_J)^{J+1} divided by the singular series coefficient.
For tuples with c = J# · k, the singular series grows like C^J, and solving the implicit equation for log x_J yields log x_J ~ J log J. This matches the empirical observation that the J-th prime in such progressions has logarithm around J log J, which is much smaller than 2^J. Under this heuristic with the correct lower bound from Hardy-Littlewood, the irrationality of the constant follows.
The key slack in the argument is substantial: I only need the first occurrence height x_J to satisfy log x_J = o(2^J), which allows the counting function to be as weak as x / (log x)^{J^{100}} or even permit first occurrence as late as exp(o(2^J)). Computing the margin: if the count is at least x / F(J, log x) for large enough x, then the first occurrence satisfies log x_J ≪ log F(J, ·), so I need log F(J) = o(2^J), meaning F can grow as large as exp(o(2^J)).
This means the required uniformity in the Heilbronn-Landau sense is remarkably weak—essentially just needing each J-run to have equal gaps below height exp(ε_J 2^J) where ε_J → 0, though there's a control issue with the aftermath term: I need |delta_{n+J} - c| < 2^J/B, but c is roughly J# ~ e^{J(1+o(1))}, and comparing e^J against 2^J creates a potential problem since e^J dominates 2^J/B eventually.
Now I'm realizing the aftermath term delta_{n+J} is typically around log p ~ J log J, which is much smaller than c ~ e^J, so |delta_{n+J} - c| ≈ c ≈ e^J, which exceeds the 2^J bound—this seems to contradict what the dossier claims unless c is actually on the order of log p_n, but at the first occurrence c ≥ J# ~ e^J, which is far larger than J log J.
Let me check the J=4 numerical example: c = 30, log p ≈ 16.1, and the squeeze identity holds exactly at the b=1 threshold of 2^4 = 16, so |delta - 30| comes in just under 16, placing delta_{n+J} somewhere between 14 and 46, likely around 15, which gives |delta - c| ≈ 15 < 16, matching log p ≈ 16 since c = 30.
Now I'm checking whether the primorial bound works: for the aftermath to stay below 2^J, I need c ≤ 2^J + O(log p), but the (J+1)-primorial grows like e^{(1+o(1))J}, which is e^J versus 2^J = e^{0.693J}—so the primorial dominates for large J. Even at J=4, the primorial c=30 already exceeds 2^4=16, so the constraint fails immediately. So the constraint forces delta to be large and close to c, which means delta_{n+J} must fall within a narrow band around c. Looking at a concrete example near p ≈ 10^7 where delta ≈ 14-15 and c ≈ 30, the gap between them is about 15.6, which fits within the logarithmic scale of 16 — this works because c naturally lands around 2^J plus the average gap size, making the whole system self-consistent.
For larger J values, the requirement becomes even more stringent: delta must exceed c minus 2^J/B, which means delta_{n+J} needs to be enormous, roughly e^J itself. This would require an exceptionally large desert of gaps immediately following the run, where the weighted average of those future gaps reaches e^J — a scale far larger than the typical gap size at that height.
Comparing this to Cramér's conjecture, which allows maximal gaps up to log² p ~ J² log² J, we see that e^J vastly exceeds this bound. But there's a key insight: we don't need the first occurrence of such a configuration; we can position p at the maximum allowed height where log p ≈ 2^J/B, which shifts the typical delta down to roughly log p ≈ 2^J/B as well. So the window for log p needs to fall between e^{J/2} and e^{0.693J}/B, which is actually nonempty for large J since 0.5 < 0.693. This suggests a wild scenario: a J-run at the right height followed by a gap-desert that pushes delta_{n+J} into the narrow band around c. The probability of delta landing in such a small interval of size 2^{J+1}/B under Cramér randomness seems vanishingly small, with the delta distribution scaling around log p ~ e^{0.6...}.
But here's where it gets interesting: c ~ e^J sits deep in the tail of delta's distribution where typical values are only e^{0.6J}, so hitting it carries a large-deviation penalty of roughly exp(-e^{0.4J}). Even though there are only ~exp(2^J) available integers n to try, the number of trials exp(e^{0.693J}) actually outpaces the exponential suppression exp(-e^{0.4J}) since 0.693 > 0.4, so the expected number of successes still grows.
For the J-run itself with gaps exactly equal to c ~ e^J, the probability per integer n is roughly exp(-J·e^{0.307J}), which is subdominant to the trial count exp(e^{0.693J}), so the heuristic still holds and Cramér's model seems plausible. Yet the dossier insists that typical aftermath forces J > log₂ log n + log₂ b + O(1), and claims the Cramér model gives log N(J) ~ J log J with vast headroom.
But I'm realizing the dossier may have overlooked that c ≥ (J+1)# ≈ e^J ≫ 2^J for large J, which fundamentally shifts the mechanism: the aftermath |δ - c| cannot stay ~ log n for large J; it must be ~ c ~ e^J unless δ is conspiratorially enormous. Let me verify the divisibility constraint: a J-run consists of J equal gaps c with J+1 primes in arithmetic progression p, p+c, ..., p+Jc all consecutive. For any prime q ≤ J+1, if q doesn't divide c, then one of these J+1 terms must be divisible by q, and the residues p + jc mod q cycle with period q among the J+1 positions. So the aftermath can't actually be O(log p_n) when c grows exponentially with J — the gap between c and the delta would be dominated by the exponential term, not the logarithmic one. This means the dossier's claim about typical runs having small aftermath is contradicted by the lower bound on c, unless I'm misunderstanding what "aftermath" means in their context. — so the corrected estimate for log N_L(J) should be around e^{J/2} rather than J log J, since achieving delta ≈ c requires a cluster of huge gaps whose cost grows exponentially with e^{J/2}. I need to recalculate what's actually needed for delta_{n+J} to land near c. Now I'm working through the probability calculations for different scenarios—the running phase, the desert phase, and landing in the target window—each with their own exponential decay rates. I need to figure out how many valid n values exist up to N and what the expected count is when maximizing over the parameter L, though I'm realizing the constraints on L are less restrictive than I initially thought since the aftermath can be arbitrarily small.
The key insight is that for the expected count to be at least 1, I need L to be at least around e^{J/2}√J, and this lower bound is what determines the first occurrence. Since log N scales with L itself, the overall growth of N(J) is roughly e^{J/2}√J, which I'm checking against the upper bound e^{0.693J} to see if this window is actually nonempty for large J.
So the Lemma does hold, but the actual Cramér profile is completely different from what the dossier claimed. The dossier said log N ≈ J log J with vast headroom to 2^J, but that was wrong—it missed the primorial constraint on c. The real headroom is much tighter: the exponent ratio is 0.5 versus 0.693 rather than J log J versus 2^J. This is a genuine correction I need to present with proof. I'm looking for explicit bounds on θ(x) from classical results—Chebyshev's work gives something like θ(x) ≥ (log 2)·x, and I can derive a lower bound using the binomial coefficient argument with primorials to show θ(x) grows at least linearly. For my purposes, I just need the bound c >= 2^{(1+ε)J} to hold eventually, which follows from the Prime Number Theorem giving θ(J+1) >= (ln 2 + ε)(J+1) for large J. Chebyshev's explicit bounds give θ(x) >= 0.92x - O(√x), so I'll use that for the writeup.
Now I can state the main result: for any J-run with p_{n+1} > J+1, every prime q <= J+1 must divide c, which means c >= (J+1)# = e^{θ(J+1)}, and by the Prime Number Theorem or Chebyshev bounds, this gives c >= e^{(1+o(1))J}, which is much larger than 2^J.
This leads to a corrected version of item 4: for sufficiently large J, any run that fits within the squeeze constraint at the odd part b — meaning |delta_{n+J} - c| < 2^J/b — forces delta_{n+J} to be exponentially large in J, which creates a tension with how delta is bounded by the supremum of future gaps.
Specifically, delta can be approximated as roughly the maximum of the first ~log(delta) gaps plus a tail term, so we'd need a gap of size around e^{J(1+o(1))}/2 to appear shortly after the run. Comparing this against known unconditional bounds on prime gaps — like Baker–Harman–Pintz giving g ≪ x^{0.525}, or even Cramér's conjecture with g ≪ log² p — gives a lower bound on where such a large gap would have to occur.
Under Cramér's conjecture, this forces log p_n to be at least e^{(1/2 - o(1))J}, which means log N(J) grows double-exponentially in J. That directly contradicts what the literature claims about N(J) growing like J log J. Without assuming Cramér, we still need gaps around e^J at the relevant height, which unconditionally gives only a weak bound of log p ≥ (1-o(1))J. But under the Riemann Hypothesis with g ≪ √p log p, the constraints tighten further, pushing p up to around e^{2(1-o(1))J}.
So the mechanism in (R) isn't actually refuted—it survives, but the quantitative picture shifts dramatically. Usable runs are those immediately followed by a gap desert of scale e^J, sitting at heights where log p falls in a specific range under Cramér-type bounds, with a positive margin of (log 2 - 1/2)J in the exponent. This is a sharper correction and a more refined conditional pathway. There's also a lower bound consideration with the aftermath, though the details are still settling.
Now I'm reconsidering the small-J regime... For a CPAP-3 with c = 6 and delta_{n+2} < 10, I need the weighted-average future gap to stay well below log p, which requires a cluster of small gaps immediately after. The probability of this happening at height p scales like p^{-c} for some constant c, and summing over all CPAP-3 occurrences (which have density roughly x/log³x) gives a convergent or divergent series depending on the exponent—so I'm checking whether Borel–Cantelli applies to show infinitely many such configurations exist. The key insight is that I don't actually need large J to make hypothesis (R) work—even with J=2 and c=6, the gap delta can land arbitrarily close to 6 with probability roughly 1/(b log p) per trial, and with infinitely many candidates this happens for every bound B heuristically. Large J was really just chosen to make the aftermath control more robust, but it's not essential to the argument itself. So delta's distribution is roughly exponential with scale around log p, and the aftermath magnitude is typically on the order of log p, which grows like e^J. But the SIZE bound requires aftermath < 2^J/b ≈ e^{0.693J}/b, and since e^J dominates e^{0.693J}, this fails generically — I'd need the aftermath to be much smaller than typical, which happens with probability roughly e^{-0.307J}/b per occurrence.
Now looking at how many runs could actually satisfy the gap condition: if c ≈ log p ≈ e^J, then the probability of hitting a gap of exactly that size is exponentially small in J, but there are exponentially many (in e^J) possible runs to check, so the count scales like e^{e^J} — astronomically large, which heuristically makes this feasible against polynomial losses. The Cramér-corrected landscape suggests usable runs exist at log N(J) ≈ e^J times some constant factor, provided c matches delta, which circles back to needing log p ≈ e^J.
But here's the tension: the allowed budget is only 2^J ≈ e^{0.693J}, which is much smaller than e^J when J is large. The aftermath bound requires staying below 2^J/b, yet typical fluctuations at height log p ≈ e^J dwarf this threshold. The probability per run becomes e^{0.693J}/(b·e^J), which is still manageable — but the real issue is that the constraint "aftermath = O(log p)" isn't actually necessary. When c ≈ log p, the condition |delta - c| ≤ C log p is trivially satisfied, so the dossier's original framing was misleading about what's truly required. So Lemma L only holds in a narrow regime where the log of the prime gap sits between e^{J/2+o(J)} and o(e^{0.693J}), with the expected first occurrence at e^{(1/2+o(1))J} — still comfortably below 2^J. The exponent gap between 0.5J and 0.693J is tight but workable. What's striking is that the numerical case with J=4 and c=30 reveals the primorial structure (30 = 2·3·5, the product of the first J+1 primes) embedded directly in their data, which they apparently missed as an asymptotic signal.
This fundamentally breaks the HL-conditional approach as originally framed. The quantitative HL strategy using tuple counts for arithmetic progressions gives runs at log N ≈ J log J, but those runs don't help the squeeze because the aftermath scales like e^J, which dominates 2^J. To salvage the conditional squeeze, I'd need to combine HL-type control of runs with prescribed gap deserts — essentially a "tuple plus desert" hypothesis where you get a desert of length roughly e^J positioned at a height where log p ≈ e^{J/2}, creating a tension between desert length and the square of the logarithm.
What's striking is that this configuration lands exactly at the Cramér-maximal-gap scale: the desert length e^J equals (log p)², which is precisely the Cramér-extremal regime. So the conditional theorem would require a hypothesis at or beyond Cramér's conjecture — gaps of size (1+o(1))·(log p)²·(const) — and the freedom to choose the constant k in c = k·(J+1)# becomes crucial for controlling the required desert size. (J+2)e^J/L exponent, with roughly e^L sites available, so I need L² ≥ (J+2)e^J, giving L ≥ √(J+2)·e^{J/2}—which matches the earlier bound L* ≈ √J e^{J/2}. The upper constraint L ≤ e^{0.693J}/(Cb) is also satisfied for sufficiently large J depending on b.
Under Cramér-with-constants style hypotheses, the mechanism closes with an exponent margin of about 0.19J. However, the actual requirement goes far beyond Hardy–Littlewood: I need to count or establish existence of consecutive-prime arithmetic progressions with common difference c ≈ (log p)²—essentially J+1 consecutive maximal-order gaps that are equal—plus controlled behavior afterward. Standard conjectures like Hardy–Littlewood uniform tuples don't directly apply because the tuple diameter grows with p (roughly J log² p), requiring uniformity in diameter up to log^{2+o(1)} p, which is beyond the fixed-tuple regime.
This points to needing a stronger hypothesis: Hardy–Littlewood uniformity for k-tuples (where k ~ loglog-ish) with diameter up to (log x)^{2+ε}. It's stronger than standard conjectures but weaker than full Cramér randomness. Actually, reconsidering the squeeze condition |delta_{n+J} - c| < 2^J/b: given that c ≥ (J+1)#, either delta ≈ c (the desert route) or delta must fall within 2^J of c ≈ e^J—so delta is forced to be close to c.
But if J stays bounded (say J = 2, c = 6), then for any b we need |delta_{n+2} - 6| ∈ (0, 4/b), which is an equidistribution problem: infinitely many CPAP-3 sites with delta_{n+2} arbitrarily close to 6. Under Cramér heuristics, delta_{n+2} should have a continuous-like distribution with positive density near integer values.
So here's the alternative hypothesis: for every ε > 0, there exist infinitely many n ≥ s where we have a CPAP-3 with g_{n+1} = g_{n+2} = 6 and 0 < |delta_{n+2} - 6| < ε. This would imply irrationality. The proof strategy is to take ε = 4/b from the rational S = a/(2^s b), then find such an n ≥ s to derive a contradiction.
This is much cleaner than before—hypothesis (E) is now a single, sharp statement: CPAP-3 with common difference 6 occur with tail-average delta arbitrarily close to (but not equal to) 6. The quantifier mess is gone and J is fixed at 2.
Even simpler is the J = 1 case with c = 2: hypothesis (E₁) says infinitely many twin primes have tail-average delta arbitrarily close to 2, meaning the gap pattern after the twin pair approaches 2 in average.
But here's the constraint: if delta_{n+1} is close to 2, I need the subsequent gaps g_{n+1+j} to be close to 2 for roughly log₂(1/ε) consecutive positions. The problem is that three consecutive gaps of 2 would mean p, p+2, p+4 are all prime—but one of these must be divisible by 3, so only 3, 5, 7 works. This forces any deviation from delta = 2 to be bounded below by something like 2^{-2} times the first gap that breaks the pattern, which contradicts getting arbitrarily close to 2. So the alternating gap pattern (2,4,2,4,...) gives a lower bound of 2/3 on delta_{n+1} - 2 for primes beyond 7, which means condition (E₁) fails when ε < 2/3 — that's a solid constraint. This also reveals that the J=1 route becomes impossible for b ≥ 3 since we'd need 2/b > 2/3, leaving only b=1,2 as viable, and I'm now checking whether J=2 and higher routes face similar dead ends.
For c=6, I'm exploring whether we can sustain long runs of gaps equal to 6 by examining if there's any parity or divisibility obstruction. Since 6 ≡ 1 (mod 5), a sequence like p, p+6, p+12, ... covers all residue classes mod 5 within 5 steps, so there's no obvious modular barrier preventing arbitrarily long 6-runs.
However, beyond about 4-5 consecutive gaps of 6, we'd need c = 30 to maintain the pattern further. This means delta can only approach 6 to a precision of roughly 2^{-4} or 2^{-5} before being forced to deviate. The key question is whether those deviations could cancel out—if the differences (g_j - 6) can be both positive and negative, their weighted sum might still equal zero, allowing delta to stay arbitrarily close to 6 despite the forced deviations. The key insight is that |delta - 6| can shrink either through 6-run prefixes that recursively approach 6 (limited to runs of length 4), or through sign-canceling mixtures where terms like (-4)/2 and 8/4 combine to exactly zero, leaving only a tail that must then satisfy the recursion again. So there are really two distinct approaches here: one focusing on fixed small parameters with delta converging to integer approximation (the equidistribution flavor), and another exploring large J with primorial arithmetic progressions (the extreme-configuration flavor). Both paths lead to irrationality but remain open questions. The key insight is that (E₁) provably fails when ε < 2/3 for b ≥ 3, which means c = 2 isn't sufficient—we need c = 6 to make it work. And more generally, I don't need c to stay constant across all n; instead, for each base b separately, I just need to find one valid configuration with appropriate n, J, and c.
Now I'm checking some concrete cases to verify the constraints. For the triple p, p+2, p+4, exactly one must be divisible by 3 since they cover all residue classes mod 3, so only p = 3 works, giving (3, 5, 7). For a CPAP with c = 6 and length 5, the terms modulo 5 cycle through all residues, forcing one to be divisible by 5, which means p = 5 gives the famous (5, 11, 17, 23, 29)—all prime and consecutive. For larger primes, the constraints tighten further.
I'm also wondering whether I can prove something stronger: that delta_n > 2 for all n, and whether the liminf of delta_n is at least 8/3.
To get lower bounds on delta, I'm using the constraint that we can't have two consecutive 2s in the gap sequence (for heights > 7), and checking what happens with the (2,4) pattern. If I greedily minimize the sum Σ g_j 2^{-j} using alternating 2s and 4s, the odd-indexed terms contribute 4/3 and the even-indexed terms also contribute 4/3, giving a total of 8/3.
Now I need to verify whether this (2,4,2,4,...) pattern stays admissible across other prime moduli. Looking at the residues modulo 5, the pattern {0,2,6,8,12,14,...} maps to {0,2,1,3,2,4,...}, which avoids hitting all residues simultaneously—this works mod 5. But as the pattern continues, I'm seeing the residues cycle in a way that needs careful tracking to ensure no prime divides all the gaps. The gap pattern (2,4,2,4) forces a deviation at the 6th term, so I need to find the minimum possible gap by computing over all admissible sequences modulo small primes. This becomes an optimization problem: minimize the average gap subject to covering constraints from each prime, where admissibility (satisfying all prime modular conditions) provides a valid lower bound for the infimum of delta. I'm testing whether the (2,4) repeating pattern achieves the lower bound of 8/3, then checking if adding mod 5 constraints yields a slightly better bound by constructing longer admissible chains that avoid both modular restrictions. The key insight is that only the first few gaps matter due to exponential decay in the weights, so I can compute the optimal δ* through an LP relaxation over those initial gaps plus a tail bound. Let me verify the repeating pattern (2,4,2,4,6) works by checking the partial sums modulo 5 to ensure no residue class gets fully covered. I'm exploring whether a repeating period can avoid hitting all residues mod 5 indefinitely. A 6-step cycle with sum 18 ≡ 3 mod 5 shifts the residue class by 3 each period, so it eventually covers everything. I need the period sum to be ≡ 0 mod 5 to stay within one residue class, but even with a longer repeating pattern like (2,4,2,4,...) summing to 30, the internal residues still end up covering all classes mod 5.
Now I'm shifting to a different approach: instead of trying to force a single residue class, I'm looking at which residues mod 15 can be avoided entirely by choosing one residue to avoid mod 3 and another mod 5. This gives 8 allowable residues mod 15. The gaps between consecutive allowed residues must all be even for the chain to work, which means all terms must have the same parity. Restricting to odd residues that avoid 0 mod 3 and 0 mod 5 gives a set mod 30 with gaps of 6, 4, 2, 4, 2, 4, 6, 2 that sum to 30.
Now I'm computing the weighted sum of gaps for different rotations of this periodic sequence, using powers of 2 as weights to find which starting position minimizes the total cost. Starting at 11 gives a gap sequence of 2, 4, 2, 4, 6, 2, 6, 4, and I'm calculating the weighted value by summing each gap divided by 2 to the power of its position, then scaling by the geometric series factor for period 8.
The calculation yields approximately 2.78, which exceeds the lower bound of 8/3 ≈ 2.67, confirming the mod-3 argument holds. Additional prime moduli introduce small corrections, but I should be careful not to overstate the exact limiting value—the key result is that the liminf is provably at least 8/3.
Now looking at the corollary: when delta_n is rational with denominator b, the constraint |delta - 2| ≥ 2/3 forces b ≤ 2, which means the J=1 case only works for b ∈ {1, 2}. This connects back to the squeeze argument and shows why larger denominators fail. So for delta_n < 4 to hold, I need g_{n+1} ∈ {2, 4} since larger gaps force delta above 4. When g_{n+1} = 4, the constraint becomes delta' < 4 recursively, and when g_{n+1} = 2, I get delta' < 6 — this creates a recursive structure where the gap sequence must stay tightly bounded to keep delta below 4 infinitely often. So I need to check whether a specific gap pattern like (2,2) is actually forbidden, and I'm working through the recursive bound: if I have a small prefix of gaps followed by any tail, the delta value compounds through nested divisions, growing unbounded unless I can control the tail. The key insight is that I need roughly log₂(p) consecutive gaps staying small—essentially a chain of prime quadruplets—to keep the exponential decay 2^{-K} from being overwhelmed, but the density of such long chains seems vanishingly small.
Now I'm calculating the probability more carefully: each gap has probability roughly 1/log p of being in the {2,4} range, so K consecutive gaps have probability around (C/log p)^K. With K ≈ log₂(log p) gaps needed, this gives an exponential decay that's strong enough for Borel-Cantelli convergence—meaning delta_n < 4 infinitely often should be false under the Cramér model, which contradicts what I'd expect heuristically.
But wait, I'm undercounting the number of n values at each scale. At scale L there are roughly e^L values of n, and even with polynomial-log losses from the probability calculation, the exponential growth dominates, so the sum diverges—meaning delta_n < 4 infinitely often is actually true with probability 1. That resolves the tension. So the dyadic case is ruled out, and achieving delta < 4 requires g_{n+1} ∈ {2,4}, which means I need to examine what minimal gap structure is necessary.
Now I'm seeing a clean reduction: if S has a dyadic denominator in ℤ[1/2], then delta_n ≥ 4 for all sufficiently large n, which rules out any n with both g_{n+1} = 2 and delta_{n+1} < 6. The contrapositive is sharp—if infinitely many n satisfy delta_n < 4, then S cannot have dyadic structure. And I can trace through specific gap patterns like (2,4,2,4,2) to bound how delta propagates forward. So I'm refining the bound by setting k = 2 log₂ p to create a window of gaps, requiring that all gaps in this window stay below 44, which gives me intervals of length roughly 127 log p that are prime-rich.  This means I need consecutive small gaps across a stretch of about 2.9 log p primes, which is a density question that's likely open unconditionally, but the key reduction is clean: either δₙ < 4 infinitely often (which would make S non-dyadic), or I can establish a finite criterion based on the gap pattern bounds.
Now I'm trying to bound δₘ unconditionally using a telescoping sum of gaps, where each gap is controlled by Bertrand's postulate, but the naive approach diverges — I need a tighter argument that doesn't blow up when summing over all the gap terms. So I've established that delta_m is bounded by a constant times p_m, which means I can use this to constrain when delta_n stays below 4. The key insight is that if all gaps in a range stay in {2,4}, they'd need to alternate in a specific pattern, but modular arithmetic forces a deviation within any 5-term window—gaps can't remain in {2,4} for more than 4 consecutive terms, so achieving delta < 4 through this approach hits a fundamental obstruction.
Now I'm setting up an automaton to track the constraint delta_n < 4 more precisely. The state represents the required bound B on the current delta value, starting at B₀ = 4. When I apply a gap g, the bound transforms according to B' = 2B - g. From the initial state B = 4, choosing g = 2 gives B' = 6, and g = 4 gives B' = 4. But from B = 6, the possible gaps {2,4,6,8,10} produce bounds that keep growing unbounded—the state space explodes rather than stabilizing.
This suggests the constraint is eventually satisfiable: there exist finite prefixes that make delta_n < 4 work. For instance, with the prefix (2,4,2,4,6,6), I can compute delta_n = 2.78125 + a remainder term that's bounded by 2^{-6} times delta_{n+6}. For this to stay under 4, I need delta_{n+6} < 78, which constrains all subsequent gaps to be at most around 76 for a sufficiently large lookahead window K determined by when 2^{-K} times a constant becomes negligible.
So I have two clean propositions: if infinitely many n satisfy delta_n < 4, then S is irrational; and delta_n < 4 holds whenever a finite explicit gap condition is met over roughly log₂(p_n) consecutive gaps. Both are proved, though the main hypothesis remains open—this feels like solid material for the deliverable.
I should also check the vacuity claim they asked me to challenge independently, looking at whether any congruence pattern on gaps actually follows from the assumptions. So the parity pattern continues: all m_n are even for n ≥ s+1, meaning b delta_n must be even, which is a stronger constraint than just being an integer. When I push to modulo 4 analysis, the pattern breaks down since g varies mod 4 without a consistent structure, so there's no fixed divisibility pattern at that level.
The key insight is that this evenness condition tightens the squeeze threshold by a factor of 2. Since b(delta_n - c) must be an even integer when c is even, and we need |b(delta_n - c)| > 0 but less than 2, the only possibility is that it equals 0. This improves the convergence bound to |delta_{n+J} - c| < 2^{J+1}/b, giving us a genuine factor-2 improvement in the squeeze condition. So at the J=4 numerical run with p = 9,843,019, the value |delta - 30| ≈ 15.6 is comfortably below the threshold of 32, which definitively rules out S = a/2^s for all s up to n* - 1 (where n* ≈ 655,000). For the dyadic case b = 1, I'm verifying that m_n = delta_n stays even for n ≥ s+1, and the scaled difference |delta_{n*+4} - 30| = 15.6/16 is strictly between 0 and 2, confirming the contradiction.
This contradiction means s must be at least n*, so S cannot be dyadic with s < n*—a result that's already evident from the digit structure, but the factor-2 refinement provides a genuine lemma-level strengthening. I'm also checking the mod-4 structure: m_{n+1} ≡ -bg_{n+1} (mod 4), and for n ≥ s+1 with m even, the residue of m_{n+1} mod 4 is entirely determined by g_n mod 4, so there's no additional degree of freedom to exploit here.
The parity constraint gives us the full extractable gain, and this refines the rationality criterion: S is rational if and only if there exists an odd b and n such that b·delta_n lies in 2ℤ—but the converse shows that b·delta_n ∈ ℤ with any parity suffices for rationality, so the 2ℤ requirement is a necessary refinement only in the forward direction.
For the (E₁)-refutation with J = 1, I need |delta_{n+1} - 2| < 4/b, and since |delta - 2| > 2/3, this bounds b ≤ 5, so b ∈ {1, 3, 5}. The J=1 route can now potentially reach b = 3 and b = 5, with delta_{n+1} constrained to (2, 2 + 4/b)—for b = 3 that's (2, 3.33), and I'm checking whether delta stays below 3.33 while maintaining the twin condition.
Now I need to verify whether liminf delta is actually ≥ 8/3 versus delta > 2 for all n, by examining the minimum value over admissible tail sequences that avoid consecutive 2s in the gap pattern, where the candidate (2,4,2,4,...) achieves 8/3.
I'm setting up a dynamic programming approach to find the minimum value across all valid gap sequences, tracking states based on whether we just played a 2 (which restricts the next gap to be ≥ 4) versus being in a free state where any even gap ≥ 2 is allowed. So the constraint that no two consecutive gaps can both be 2 only applies when the middle prime exceeds 5, which means it holds for all subsequent primes once we're past n=3 where p_{n+1} ≥ 7. This gives us delta_n ≥ 8/3 for n ≥ 3, and the bound is actually strict since achieving equality would require the pattern (2,4) repeating infinitely, which violates the mod-5 constraint. For the small cases delta_1 and delta_2, I need to compute those numerically. Now I'm verifying the upper bound constraints for the case where c = 4, checking that delta values near 4 don't create obstructions on either side. The key insight is confirming my main claim about J-runs implying J+1 consecutive primes in arithmetic progression starting from p_{n+1}.
For the divisibility argument: if a prime q ≤ J+1 doesn't divide c, then as j ranges from 0 to J, the residues jc mod q cover all possibilities (since q doesn't divide c and J ≥ q-1). This means some j₀ ≤ J makes p_{n+1} + j₀c divisible by q, forcing p_{n+1+j₀} = q. But if p_{n+1} > J+1, this creates a contradiction, so every prime q ≤ J+1 must divide c, which means (J+1)# divides c.
Using the prime number theorem, (J+1)# grows like e^{θ(J+1)} where θ(x) ~ x, so c ≥ e^{(1+o(1))J}, meaning c grows exponentially faster than 2^J.
But checking small cases directly: J=2 gives c ≥ 6 > 4 ✓, but J=3 fails since c could be 6 while 2³ = 8, so 6 < 8 ✗. For the J=3 case with c=6 (from known cousin prime quadruplets like 251, 257, 263, 269), the constraint becomes |δ - 6| < 16 where δ ~ log p, requiring p < e²² ≈ 3.6×10⁹.
Continuing this pattern through J=4 and J=5, I'm finding that the refined thresholds |δ - c| < 2^(J+1)/b consistently work at b=1 and match the numerical behavior, with the margin tightening as J increases but remaining feasible for typical gap sizes. Looking at the feasibility window around log p ≈ 210, I'm checking whether J=7 runs at CPAP-8 with c=210 are actually viable—the Cramér probability per site comes out to roughly 10^{-19.3}, but with e^{210} ≈ 10^{91} available sites, there's enough abundance to make this work. Something's shifting in my analysis here though.
I'm realizing I've been anchoring on c = (J+1)# as minimal with log p ≪ c, but the real insight is to tune the height so log p ≈ c instead. When they're balanced like this, the aftermath deviation |delta - c| scales as O(log p) = O(c), which isn't small enough—I need it well below 2^{J+1}/b ≪ c for J ≥ 6 to stay beneath typical fluctuations. The probability per run-instance becomes 2^{J+1}/(b·c), and with L ≈ c = 210, the total count works out to e^{210}·(e^{-1}/210)^7·2^7/b, which is tiny but nonzero.
Now I'm checking the scaling more carefully: this evaluates to roughly 10^{73}/b, so for any fixed b ≤ 10^{70}, the expected count exceeds 1. But the real constraint is that I need to handle ALL values of b simultaneously—as b grows, I shift to larger J where 2^J becomes sufficiently large. The general pattern shows that successes scale like exp(L - J(1 + ln L) + (J+1)ln 2 - ln b - ln L), dominated by e^L, which stays ≫ 1 for large L. This suggests that for every pair (b, J ≥ J_min), there should heuristically be infinitely many usable runs at the L ≈ c scale, provided J is large enough—though I'm still puzzling over what exactly breaks down for small b.
The aftermath-proximity constraint |δ - c| < 2^{J+1}/b is a rare event in distribution with probability ~ 2^{J+1}/(bL), which is positive, giving infinitely many candidate sites for any b and J ≥ 2 with any admissible c. But when I fix J = 2 and c = 6, the heights with log p ≈ 6 are actually finite. As n → ∞ and L = log p_n grows much larger than 6, δ scales like L, making |δ - 6| ≈ L, which needs to stay below 8/b—and this becomes increasingly unlikely.
The probability that δ ≤ 14 decays roughly like (14/L)^{log₂ L}, which is super-polynomially small but still dominated by the exponential abundance of sites per scale e^L, so usable runs remain Cramér-abundant even at (J,c) = (2,6), confirming my earlier analysis.
For the corrected landscape with J ≥ 6, the dossier's Lemma-L profile claiming O(log p) automatic-typicality is wrong because c ≥ (J+1)# ≫ 2^J forces either fine-tuned proximity events with probability ~2^J/(b log p) or a matching scale where log p ≈ c ≈ e^J that still produces proximity with aftermath ≪ typical, so in all these cases the matching behavior is fundamentally constrained.
The core insight is that the squeeze always reduces to requiring delta_{n+J} to land within 2^{J+1}/b of c exactly at the end of a (J,c)-run. For small J and c values with small b, this window can exceed the typical |delta - c| spread making it "free" at appropriate heights (like the numerical J=4 case), but as b grows the denominators force some fine-tuned proximity to appear somewhere, meaning the equal-run squeeze can only prove irrationality through an equidistribution-type event rather than mere typicality. Now I'm verifying the bound using the prime number theorem and explicit calculations—checking that θ(11) exceeds 11·ln(2), which gives me the primorial 11# = 2310 strictly larger than 2^11 = 2048, confirming the threshold at J = 10.
But when I push to J = 11, the constraint fails: θ(12) drops below 12·ln(2), and the primorial for primes up to 12 is still just 2310, which is now smaller than 2^12 = 4096. So I need to refine the approach using a parity-based window condition where the gap between the radical and the candidate c stays within 2^(J+1)/b, with additional constraints on logarithmic factors. At J=14 the primorial drops back under the power of 2, freeing up the window again, and this pattern continues through higher values—J=15 stays free, but J=16 requires fine-tuning as the primorial for primes up to 17 exceeds the corresponding power of 2 by a factor of nearly 4, then gradually becomes manageable again at J=18 and beyond, with the primorial for primes up to 20 eventually fitting comfortably within the 2^24 window.
The asymptotic behavior shows that the ratio of primorial to 2-power grows exponentially, so eventually the primorial will always exceed any fixed power of 2, but the crossover point depends on where the Chebyshev theta function stabilizes relative to the logarithmic bound—since theta fluctuates with each new prime, the exact moment when primorials permanently outpace powers of 2 is determined by these oscillations. So the comfortable free regime actually bottoms out around J = 15, where θ(16) stays safely below 2^16; beyond that the ratio starts climbing—J = 17 and 18 are marginal with factors of 3.9 and 1.93 respectively, and by J = 19 we're way off at 18.5×, so the typicality window effectively closes after J = 15.
For J ≥ 19 the deviation worsens exponentially as e^{0.307J}, which means I can formalize this as Theorem B: for all sufficiently large J (specifically J ≥ 19), the primorial bound fails systematically. I'm verifying this by checking that θ(x) > x ln 2 holds for all x ≥ 31 using known Chebyshev bounds—θ(31) = ln(200560490130) ≈ 26.02 beats 31 ln 2 ≈ 21.5, and this inequality persists for larger x by monotonicity arguments.
Now I'm spot-checking the inequality θ(x) > 0.8x for x ≥ 101 across the range 31 to 101 using primorial values: θ(37) ≈ 29.6 versus 25.6, θ(41) ≈ 33.3 versus 28.4, and continuing through θ(73) with each case confirming the bound holds. Continuing to verify the inequality across more prime boundaries, checking the margins get tighter in some regions—particularly around 31 and 37 where θ(x) barely exceeds x ln 2, with gaps as small as 0.38—but the pattern holds consistently through the higher primes up to 89. So I've confirmed that θ(x) > x ln 2 holds for all x ≥ 29, verified across the range up to 101 and supported by known bounds for larger values. This means (J+1)# > 2^(J+1) whenever J ≥ 28, and I'm also checking the smaller cases where J+1 falls between 20 and 28 to confirm the pattern holds there too.
Looking at the boundary cases more carefully, I see that the inequality actually works down to around x = 19, but there's a problematic interval between roughly 23 and 29 where θ(x) dips below x ln 2 — specifically, θ(23) = 19.22 fails against 23 · 0.693 ≈ 20.09, and the inequality remains violated until x reaches about 29.
Now checking the primorial versus powers of 2: at J = 28, the primorial 23# is actually smaller than 2^28, giving a ratio of only 0.83, but by J = 29 the primorial 29# dominates 2^29 by a factor of about 12. This confirms that for all J ≥ 28, the primorial inequality holds asymptotically with an exponential growth ratio of roughly e^{(1-ln2)J}.
The key insight is that once we establish J₀ = 28, any J-run must have c > 2^{J+1}, which forces the gap average after the run to exceed (J+1)#/2 — an exponentially large threshold. This creates a contradiction: such massive required gaps would imply prime gaps growing doubly exponentially in height, yet the actual distribution of primes contradicts this, making the existence of arbitrarily long J-runs impossible. Actually, could strong uniformity in the Maynard-Tao framework help with the proximity approach? The proximity event requires delta to land in a narrow window around c, which depends on all future gaps—essentially a K-gap pattern where K grows like log log p plus log b. That means I'd need Maynard-Tao uniformity to hold for tuple sizes scaling up to roughly log log x, which is a much stronger requirement than the fixed or poly-logarithmic sizes typically handled. Now I'm working through the gap-pattern construction: picking the initial gaps and then building a sequence of K additional gaps whose sum lands within a specific window, where the tail contribution beyond those K gaps decays exponentially. The uncertainty from that tail is bounded by roughly 2/b, which constrains K to scale like θ log p rather than log log p—so the tuple size grows logarithmically with p, not logarithmically with log p. The key tension is that unconditionally the tail uncertainty is O(p^{0.525}), but within a Hardy–Littlewood conditional framework I might be able to tighten the tail behavior.
Now I need to pin down the conditional probability: given the pattern of the first K gaps, the remaining tail delta' is bounded in a range around p^{0.525}, and to force delta into the target window I need the scaled tail 2^{-K} times that range to fit within the window width. This gives K roughly 0.76 log₂ p, so I'm looking for a tuple of about 1.1 log p primes with prescribed gaps spread across a diameter that's roughly K times the typical gap size in the pattern—which balloons to around log² p overall.
This is hitting exactly the Cramér-granularity scale that Harman-Lao work with, where k ~ log x. But here's the problem: the standard Hardy-Littlewood asymptotic is known to break down at this scale. Granville and Soundararajan have shown that the naive HL main term becomes inconsistent with itself when k ≍ log x, and Montgomery's work confirms the asymptotic fails in this regime. So I'm being forced to apply Hardy-Littlewood at precisely the scale where it's known to be unreliable.
The cleaner alternative is to sidestep the main term entirely and instead show that the gaps equidistribute modulo 1 — that delta_n hits every fixed interval infinitely often. This is unproven but much more elegant. The cleanest formulation would be a conditional theorem: assume that for every ε > 0, there exist infinitely many n where consecutive gaps stay within ε of each other, and from that hypothesis derive the result.
Now I'm working through the parity argument more carefully. If the gaps cluster near 6 with the right parity structure, then the difference b(delta_n - 6) must be an even integer. But the constraint forces this difference to be smaller than 2 in absolute value, which means it can't be a nonzero even integer — so it must be zero, contradicting the assumption that the gaps actually deviate from 6. This contradiction proves S is irrational.
The heuristic support from Cramér's model shows that the probability of observing such gaps near 6 should accumulate infinitely often, and there's no divisibility obstruction preventing gaps from clustering around 6 itself. After the run gaps, I need the weighted sum of deviations to approach zero from above, so patterns like (6, 8, 4, 6, ...) with small adjustments work—the key is showing that for any precision level K, I can construct an admissible pattern with bounded error using deviations from the set {..., -4, -2, 0, 2, 4, ...}. Writing Lean 4 formalizations... ric check of the irrationality constant and the large prime computation to verify my own work — that's local reasoning, not external research. I'll proceed with bash for the sha256 hash and a quick numerical sanity check, keeping it minimal and transparent about what's computed versus what's asserted from the dossier.
Now I need to revisit the parity refinement more carefully since it's critical for the threshold calculations — specifically, how to express S in terms of the minimal power of 2 dividing the numerator. So for n ≥ s + 2, I have b(delta_n - c) ∈ 2ℤ, and using the squeeze argument with a constant sequence g_{n+1..n+J} = c where c is even, I get that b(delta_n - c) = 2^{-J} b(delta_{n+J} - c). If the difference |delta_{n+J} - c| stays small enough, this forces b(delta_n - c) to lie in both 2ℤ and the interval (-2, 2) excluding zero, which is impossible.
This contradiction confirms the result. I'm also checking whether mod 4 structure gives anything stronger — the residue class of g_{n+1} mod 4 depends on whether consecutive primes fall in the same quadratic residue class, which behaves like a random walk with no fixed pattern, so there's no additional constraint beyond parity.
The key insight is that only one bit of information can be extracted from each gap without knowing the full gap sequence itself. I'm also connecting this to the doubling map on the circle: the irrationality of the gap-driven orbit is equivalent to the binary digit stream of the gap sum being non-periodic, where each digit is determined by whether the fractional part of the scaled orbit exceeds 1/2. Organizing the writeup as a markdown document with a claims ledger tracking fifteen statements across their dependencies and confidence levels, then structuring the main sections to build from basic identities through the rationality criterion and into the key theorems—particularly establishing the lower bound on delta_n and showing that the J=1 case fails for b ≥ 7 once we account for the parity constraint.
Now moving into section 4 on the primorial obstruction, where I need to establish that q divides J+1 and that the primorial (J+1)# exceeds 2^(J+1) for all J ≥ 28, using the Chebyshev bound θ(x) > x ln 2 for x ≥ 29—I've verified this locally with a primorial table up to around 101, but for larger x I either need to cite an existing bound or derive it elementarily from the classical Chebyshev lower bound via binomial coefficients.
I'm working through the relationship between ψ(x) and θ(x) to establish the needed inequality: using the binomial coefficient bound to show ψ(x) ≥ x ln 2 - O(log x), then accounting for the secondary terms in θ(x) = ψ(x) - ψ(x^{1/2}) - ψ(x^{1/3}) - ... to get θ(x) ≥ x ln 2 - C√x log x, which holds for sufficiently large x. I'm working through the Chebyshev approach using the weighted sum ψ(x) ≥ T(x) - T(x/2) - T(x/3) - T(x/5) + T(x/30), which yields the constant A ≈ 0.92129 — this is the classical result but proving it fully inline would be lengthy, so I'll sketch it with confidence and mark the explicit threshold as approximate. Alternatively, I could sidestep the full calculation by stating Theorem B for sufficiently large J (with J ≥ 28 from the table, and beyond 101 via Chebyshev–PNT as a classical result sketched inline).
For the literature-based claims like PNT and Chebyshev, I need to label them as [LIT-CLASSICAL, unverified inline] since the rules require flagging unproven dependencies. In §5, I'm correcting the dossier item 4 and recalibrating the Cramér headroom heuristic, then showing that the "aftermath ~ log n typicality" collapses for J ≥ 28 as a consequence of the delta-typical-size bound — phrasing this carefully as a conditional: if |δₙ₊ⱼ - c| ≤ 2^(J+1), then δₙ₊ⱼ ≥ (J+1)# - 2^(J+1), which follows trivially.
Now I'm establishing that the supremum of the gap sequence over a window gives a lower bound of roughly (J+1)# times a correction factor, which yields an unconditional gap of at least (J+1)#/4 within the logarithmic window. Pairing this with any upper bound on gaps and using the unconditional bound g_m < p_m, I derive that p > (J+1)#/4, so log p ≥ J(1-o(1)) — this is a weak proved result. Under the Cramér-type heuristic that g ≪ log²p, I get a conditional consequence that log p ≥ e^(J/2)·c, which I label accordingly as conditional. Moving into §6 to lay out the conditional theorems.
Now examining what the master condition (R') and the Poisson condition (P) imply: (R') directly proves irrationality, and (P) gives an instance of (R') that also proves irrationality. The Desert variant (D) reduces to a similar irrationality result. For the Hales-Jewett uniformity discussion, I'm clarifying what HL actually provides — quantitative HL with uniformity parameter k ≤ K(x) and diameter bounded by (log x)^(2+ε) produces runs but fails to control what happens afterward. Controlling the aftermath requires either an equidistribution input like (P) or tuple sizes k ≍ ε·log x, but at that scale HL's asymptotic reliability becomes questionable and unverified in the literature. The upshot is that there's no proof using only standard conjectures within this framework without bringing in an equidistribution component.
Moving into the delta < 4 route for the dyadic case, I've proved the implication and found a finite sufficient criterion. The equivalences section connects the doubling map orbit structure to digit representations. For the numerical verification, I'm computing S to modest precision using a sieve up to around 3 million or so — a bit array takes roughly 30MB and runs quickly in pure Python, though I might start with 1 million to hit the prime 9,843,019 and verify the J=4, c=30 quintuplet there.
I'm checking that 9,843,019 and its offsets by 30, 60, 90, 120 are all prime and consecutive, then computing delta_{n+4} via a tail sum with about 40 more primes at high precision to verify it stays within the expected bounds. The S value matches the dossier at 3.674643966011328778995676, and I'm confirming the identity sum g/2^n = S - 2 numerically, which strengthens confidence in the local verification.
The core obstacle now is finding an unconditional mechanism that forces delta_n near a prescribed even integer at specific pattern-sites, or equivalently, establishing a lower bound.
The key insight emerging is that delta_n < 4 infinitely often appears to force g_{n+1} ∈ {2,4} infinitely often, which would connect to twin or cousin primes—so the dyadic subproblem might hinge on whether this constraint is actually necessary. δ_n <= H_m + 2^{-(m-1)}δ_{n+m} bound doesn't work unconditionally since a single gap doesn't constrain δ, but Maynard's m-cluster approach gives m primes in an e^{3.815m} window infinitely often; using the Maynard-Bourgain-Hough bound δ ≪ p^{0.525}, I can set m = 0.76 log₂ p and express δ_m as a sum over gaps with exponential decay, which should give an unconditional upper bound on δ.
But this explodes—the window scaling e^{3.8m} grows much faster than any bound D I need, so the cluster route is dead. The core obstruction is that I need both a gap pattern and a tail functional in a prescribed interval simultaneously, which I can't handle unconditionally, so the real target is whether δ_n < 4 infinitely often assuming the twin prime conjecture.
Even that's unclear though—twin primes give g = 2 infinitely often, which would make δ_n = 1 + δ_{n+1}/2 < 4 if δ_{n+1} < 6 at twin sites, but I need joint control of twins with small following gaps, and that's still open even under the twin conjecture alone. The true bottleneck is this joint control problem.
Writing Lean 4 statements...
I should verify the hypothesis (P) more carefully—using a one-sided interval (6, 6+ε) guarantees the gap isn't zero, but a two-sided interval 0 < |δ_{n+2} - 6| < ε works just as well, and I need infinitely many n with n ≥ s + 2, which checks out. Also confirming the lower bound on c: if each prime q ≤ J+1 divides c, then their product divides c since they're distinct, so c ≥ (J+1)# where (J+1)# is the primorial—that's solid.
For J=4 the primorial 5# = 30 matches the data. A J-run means J equal gaps c after index n, which corresponds to J+1 consecutive primes in arithmetic progression—the definitions align. One more sanity check on the forbidden pattern: two consecutive gaps of (2,2) would require three consecutive odd primes p, p+2, p+4, which only happens at p=3, but since I'm looking at gaps starting from index n ≥ 3, the first prime involved is at least 7, so this pattern never appears in the tail.
Now I'm working through the dynamic programming argument: the sum δ_n = Σ_{j≥1} g_{n+j}2^{-j} with even gaps ≥ 2 and no consecutive (2,2) has a lower bound of 8/3, achieved only by the infinite sequence (2,4)^∞. But our actual gap sequence can't match that pattern because of a modular constraint: looking at partial sums of gaps (2,4,2,4,2...), the residues mod 5 cover all five classes, which means one of the primes in that window must be divisible by 5, forcing it to equal 5 itself—so this pattern only works for very small primes.
For n ≥ 3 where p_{n+1} ≥ 7, the covering argument shows that the repeating (2,4) pattern is impossible: any six consecutive gaps in that pattern would force one of the resulting numbers to be composite, a contradiction. This means δ_n must strictly exceed 8/3, and any deviation from the (2,4)^∞ ideal at position j incurs a quantifiable cost in the lower bound.


--- REACHED TURN LIMIT ---

Operator: 
