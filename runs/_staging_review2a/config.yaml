run_id: TODO_YYYYMMDD_model_review2a_noweb  # operator: rename dir to match
stage: review
model: "TODO"      # cross-family track (OpenAI) per ANN-08; operator picks
provider: "TODO"
thinking_budget: "TODO"
temperature: null
payload_file: payloads/review_2a.md
payload_sha256: "TODO_operator_canonical"   # steering-side: HASHES.steering.txt
object_file: object_conditional_chain.md    # in this run directory
object_sha256: "TODO_operator_canonical"    # steering-side: HASHES.steering.txt
object_derivation: >-
  Deterministic derivation: strip_review_copy.py (this directory) applied
  to dossier/chain-v1.md, both at the followup21 tip commit. Assertions
  enforce removal of prior-review verdicts, per-claim review tags, v1.1
  amendment brackets, process vocabulary, and the provenance section;
  required-content assertions guard the mathematics. Re-run reproduces
  the object byte-identically.
web_access: false
tools: [local_code_execution]   # runs/README rule 6: network OFF, local compute encouraged
contamination_class: "R (temp chat, memory OFF)"
wrapper_file: wrapper.md        # verbatim, German (house convention; Q5)
trace_artifact_layer: "TODO at archive time (rule 11: raw | rendered | summarized | undetermined)"
started: ""
finished: ""
operator: istr
notes: >-
  Instrument deltas vs review_1b, deliberate (ANN-21): computation is
  MANDATORY (checklist rule iv exported into the payload); Phase-B probes
  P1-P8 cover the load-bearing surface at uniform intensity so that no
  probe localizes a previously defective site. INTERNAL, not for the
  reviewer: sections 2-3 arithmetic of the object is machine-checked in
  lean/ (milestone, ANN-18) -- findings there estimate the reviewer
  false-positive rate; the live signal is sections 4-6. Object is
  anchor-stripped per the Q5 anchor-propagation record.
